# Obsidian Plugin Compatibility — Slatebase

**Datum:** 2026-08-25
**Scope:** Obsidian Community-Plugin-Kompatibilitätsschicht (`frontend/src/plugins/compat/`, `backend/src/plugin*`)
**Methodik:** Quellcode-Audit der Shim-Implementierung gegen die offizielle `obsidian.d.ts` (npm-Paket `obsidian`, Version 1.13.1) + statische Bundle-Analyse der 100 meistheruntergeladenen Community-Plugins (Obsidians offizieller `community-plugin-stats.json`-Feed, `manifest.json`/`main.js` des jeweils aktuellen GitHub-Release) + manuelle Tests mit echten, aus GitHub geladenen Plugin-Bundles in der laufenden App + Wartungsstatus-Audit der 100 GitHub-Repos (siehe [Wartungsstatus der Top-100-Plugins](#wartungsstatus-der-top-100-plugins-upstream-pflege))

---

## Zusammenfassung

Slatebase emuliert eine Teilmenge der Obsidian-Plugin-API (`App`, `Vault`, `Workspace`, `MetadataCache`, `FileManager`, Command-Palette, Event-System) im Browser, damit echte Obsidian-Community-Plugins im Web-Frontend laufen — ohne Electron, ohne Node.js, ohne lokales Dateisystem. Der Layer gilt weiterhin als **⚠️ experimentell**. Emulierte API-Version: **1.13.2** (APIs bis einschließlich 1.13.1 implementiert).

Von den 100 meistheruntergeladenen Community-Plugins:

| Kompatibilität | Anzahl | Anteil |
|---|---:|---:|
| Voll kompatibel | 54 | 54 % |
| Teilweise kompatibel | 35 | 35 % |
| Nicht unterstützt (Desktop-only) | 11 | 11 % |

18 der 100 Plugins wurden zusätzlich manuell mit ihrem echten, aus GitHub geladenen Bundle in der laufenden App getestet (nicht nur statisch analysiert) — Spalte „Manuell getestet" in [Top 100 Community-Plugins nach Downloads](#top-100-community-plugins-nach-downloads).

**Zwei bekannte, voneinander unabhängige Aktualitätslücken:**
1. Zwei Bereiche der emulierten API sind bewusst **typisiert, aber nicht funktional** — als nicht-abstürzende No-Ops, nicht als stille Lücken: **Bases** (1.10.x, keine Formel-Engine, kein `.base`-Rendering) und der **Desktop-CLI-Handler** (1.12.2, in einer Web-App existiert keine CLI). Der `compatibility-analyzer` stuft jedes Plugin, das diese referenziert, als „teilweise" statt „voll" ein.
2. **Unabhängig von Slatebases eigener API-Version:** ein Wartungsstatus-Audit der 100 GitHub-Repos ergab, dass 24 der 100 Plugins seit über 12 Monaten keinen Commit mehr hatten (teils archiviert/umbenannt) — bei 4 davon gibt es bestätigte GitHub-Issues, dass sie bereits unter aktuellem, echtem Obsidian nicht mehr funktionieren. Ein kaputtes Upstream-Plugin bleibt kaputt, egal wie aktuell Slatebases Emulation ist. Details: [Wartungsstatus der Top-100-Plugins](#wartungsstatus-der-top-100-plugins-upstream-pflege).

---

## Architektur (Kurzüberblick)

```
Plugin-Verwaltungsseite / Command Palette (UI)
        │
PluginLoader (Bundle laden, Lifecycle: onload/onunload, 10s-Timeout)
        │
PluginSandbox (Netzwerk-Allowlist, Storage-Namespacing, Cross-Vault-Schutz)
        │
API-Shim-Layer (App, Vault, Workspace, MetadataCache, FileManager, Commands)
        │
Event-System (on/off/trigger, EventRef)
        │
Slatebase API-Client / State
        │
Backend Plugin-Store (REST-API, dateibasierte Persistenz pro Vault)
```

- **Proxy-basiertes API-Shimming**: Nicht emulierte Property-/Methodenzugriffe auf Shim-Objekten (`App`, `Vault`, `VaultAdapter`, `Workspace`, `MetadataCache`, `FileManager`, `WorkspaceLeaf`, `FileExplorerView`, `Editor`) liefern `undefined`/No-Op zurück statt zu crashen, mit einer einmaligen `console.warn` pro Property und Plugin-Instanz. Dieselbe Garantie gilt für `fallback-shims.ts` (Warnung beim ersten Lesezugriff) und für den Plugin-Sandbox-`require()` bei unbekannten Node-Modulen. Jeder dieser Zugriffe landet im `api-gap-registry` und ist zur Laufzeit über `window.__slatebasePluginApiGaps()` abfragbar — getrennt nach *gelesen* (oft harmlose Feature-Detection) und *aufgerufen* (das Plugin hat mit Wirkung gerechnet und bekam keine).
- **Kein Web Worker**: Plugins erwarten synchronen DOM-Zugriff; die Sandbox arbeitet über API-Interception und Monitoring, nicht über Worker-Isolation.
- **Vault-scoped Instanzen**: Jedes Plugin bekommt pro Vault eine eigene `AppShim`. Bei Vault-Wechsel: unload → neu instanziieren → load.
- **Emulierte API-Version**: `1.13.2` (APIs bis einschließlich 1.13.1 sind implementiert). Plugins mit höherer `minAppVersion` gelten als inkompatibel. Bases (1.10.x) und der Desktop-CLI-Handler (1.12.2) sind bewusst nur typisiert, nicht funktional — Plugins, die sie referenzieren, stuft der `compatibility-analyzer` als „teilweise" ein.
- **Lazy Loading**: Plugins laden asynchron nach dem ersten Rendering (First Contentful Paint), um die Ladezeit nicht zu beeinträchtigen.

---

## Was wird unterstützt

### Vault
`read`, `modify`, `create`, `createFolder`, `delete`, `rename`, `trash`, `copy`, `process`, `append`, `exists`, `cachedRead`, `getAbstractFileByPath` (+ `Insensitive`), `getFileByPath`, `getFolderByPath`, `getMarkdownFiles`, `getFiles`, `getAllLoadedFiles`, `getAllFolders`, `getRoot`, `getName`, `getConfig`, `getAvailablePath(ForAttachments)`, `getResourcePath`, `createBinary`/`readBinary`/`modifyBinary`, Events (`on`/`off`/`offref`/`trigger`). `vault.adapter` implementiert Obsidians `DataAdapter`-API (`exists`, `read`, `write`, `list`, `stat`, `mkdir`, `remove`, `rename`) für Plugins, die direkt am Adapter statt an der Vault-API vorbeigehen; darüber hinausgehende `DataAdapter`-Methoden (`copy`, `getResourcePath`, `process`, …) sind proxy-geschützt (gelb, warnt, kein Absturz).

### MetadataCache
`getFileCache`, `getCache`, `getFirstLinkpathDest`, `resolvedLinks`, `unresolvedLinks`, `fileToLinktext`, `getTags`, `blockCache`, `getBacklinksForFile`, `isUserIgnored`, Events. `CachedMetadata` wird vollständig befüllt: `frontmatter`, `tags`, `links`, `blocks`, `headings`, `embeds`, `sections`, `listItems`, `footnotes`/`footnoteRefs`, `referenceLinks`, `frontmatterLinks`. Parsing läuft synchron on-demand nach jedem `vault.read()`. `getBacklinksForFile` invertiert dieselbe Link-Auflösung wie `resolvedLinks` und liefert `{ data: Map<Quellpfad, LinkCache[]>, count() }`. `isUserIgnored` liefert immer `false` — Slatebase hat kein "Excluded files"-Setting, was exakt Obsidians eigenem Default bei leerer Ausschlussliste entspricht.

### Workspace / Leaf-API
Tabs und Sidebar-Panels bilden Obsidians Leaf-Konzept ab: `getLeaf`, `getLeavesOfType`, `getActiveViewOfType`, `getActiveFileView`, `getMostRecentLeaf`, `revealLeaf`, `detachLeavesOfType`, `getActiveLeaf`, `setActiveLeaf`, `getRightLeaf`/`getLeftLeaf`, `openLinkText`, `getUnpinnedLeaf`, `iterateAllLeaves`/`iterateRootLeaves`, `iterateLeaves` (Legacy-Alias auf `iterateAllLeaves`), `onLayoutReady`, `onLayoutChange` (Legacy-Alias, feuert direkt das `layout-change`-Event), `registerHoverLinkSource`/`unregisterHoverLinkSource`/`hoverLinkSources` (Lesezugriff auf dieselbe Registry), Events (`active-leaf-change`, `layout-change`, `file-open`, `editor-change`, `resize`). Plugin-Views laufen als eigene Tabs im Hauptbereich (virtueller Pfad `__view::{viewType}`), als Sections im rechten Context Panel (`getRightLeaf()`) oder als Sections im linken Sidebar-Panel (`getLeftLeaf()`) — beide Sidebar-Seiten sind eigenständige Leaf-Locations mit eigenem Tab-/Split-System. `getActiveFileView` ist eine echte (undokumentierte, aber stabile) Obsidian-Internal-API und liefert dieselbe View wie `getActiveViewOfType(FileView)` — keine Degradation. `workspace.activeLeaf` ist wie in echtem Obsidian praktisch nie `null`: Plugin-Code dereferenziert routinemäßig `leaf.view`, ohne `leaf` selbst auf `null` zu prüfen (z. B. Excalidraws `isUnwantedLeaf()`/`onActiveLeafChangeHandler`). Statt eines rohen `null` liefert Slatebase in Zuständen ohne aktive Datei ein reales, aber inhaltsloses Leaf mit `empty`-View (`getOrCreateEmptyLeaf()`), an dem `leaf.view` nicht crasht.

**Graceful Degradation statt Fehler**: Methoden, für die Slatebase keine echte Entsprechung hat, aber deren Ersatzverhalten offensichtlich und harmlos ist, gelten als *unterstützt* und loggen zur Laufzeit, was sie stattdessen tun — kein Kompatibilitäts-Warnhinweis vor der Installation:
- `createLeafBySplit`/`splitActiveLeaf` → neuer Tab statt echtem Split
- `openPopoutLeaf` → normaler Tab statt Popout-Fenster
- `getLayout`/`rootSplit`/`leftSplit`/`rightSplit`/`floatingSplit` → Stub-Objekte
- `iterateCodeMirrors` → Callback wird nie aufgerufen (kein CM5 in Slatebase)
- `protocolHandlers`/`protocolHandler` → echte, beschreibbare `Map`/Feld, aber nie aufgerufen (Web-App kann kein `obsidian://`-URI-Scheme vom OS empfangen)

### Direkter DOM-Zugriff auf den Datei-Explorer
`workspace.getLeavesOfType('file-explorer')` liefert ein synthetisches Leaf mit `view.fileItems: Record<path, { file, titleEl, titleInnerEl, selfEl }>`, und die Zeilen-DOM in `TreeNode.tsx` trägt zusätzlich `data-path`/`nav-folder-title(-content)`/`nav-file-title(-content)`. Damit funktionieren beide DOM-Pfade, über die Plugins wie **Iconize** (`obsidian-icon-folder`) ihr Icon-Element einfügen: `fileItems[path]` → `titleEl` sowie direktes `document.querySelector('[data-path="…"]')`-Scraping. `titleInnerEl` (real Obsidians `titleInnerEl`/`innerEl`) zeigt auf den `.nav-*-title-content`-Wrapper und ist Pflicht für Iconizes `addAll()`-Reload-Pfad, der per `titleEl.insertBefore(iconNode, titleInnerEl)` einfügt — fehlt das Feld, behandelt `insertBefore` das `undefined`-Referenzelement wie `null` und hängt das Icon ans Ende statt davor an (Icon landet hinter statt vor dem Dateinamen). Dateizeilen (`.tree-node-file`) sind `display: flex`: Ein zusätzlich eingefügtes Plugin-Icon-Element ist damit ein Flex-Geschwister neben dem `.nav-file-title-content`-Wrapper statt eines Block-Kindes, das in eine eigene Zeile umbricht.

Vier Abweichungen von echtem Obsidian, bewusst in Kauf genommen:
- **`fileItems` enthält nur aktuell im DOM gerenderte (= aufgeklappte) Zeilen**, nicht den kompletten Vault-Baum. Echtes Obsidian hält alle Datei-Items unabhängig vom Aufklapp-Zustand vor; ein Ordner zuklappen entfernt seine Kinder hier aus `fileItems`, als gäbe es sie noch nicht.
- **Umbenennen verliert injizierte Plugin-DOM**: Die Zeile hängt am React-`key` (`node.path`); ändert sich der Pfad, erzeugt React einen neuen DOM-Knoten. Betrifft Iconize nicht kritisch, da es auf `vault.on('rename')` ohnehin neu reagiert.
- **Pfade sind nicht vault-eindeutig**: `fileItems` ist nach vault-relativem Pfad indiziert (wie Obsidians eigenes Single-Vault-Modell). Slatebases Seitenleiste kann mehrere Vaults gleichzeitig aufgeklappt zeigen — überschneiden sich Pfade zweier offener Vaults, gewinnt die zuletzt gemountete Zeile.
- **Iconize-eigenes CSS greift im Explorer nicht automatisch**: Der `CssInjector` scoped Plugin-Styles auf `[data-plugin-id="…"]`-Subtrees, die der Datei-Explorer nicht trägt. Ein injiziertes Icon-Element erscheint also zusätzlich zum Slatebase-eigenen Icon, nicht als dessen Ersatz.

### FileManager
`renameFile`, `processFrontMatter`, `generateMarkdownLink`, `getNewFileParent`, `createNewMarkdownFile`, `createNewFile`, `createNewFolder`, `createAndOpenMarkdownFile`, `promptForFileDeletion`, `promptForFolderDeletion`, `trashFile`, `getAvailablePathForAttachment`. `createNewFile` erlaubt beliebige Extensions (nicht nur `.md`); `createNewMarkdownFile` ruft es intern nur noch mit `'md'` auf. `createNewFolder` generiert bei einer Namenskollision einen eindeutigen Namen (Suffix `1`, `2`, …), analog zu `createNewFile`, da `vault.createFolder()` anders als `vault.create()` bei existierendem Pfad wirft statt „create or get" zu verhalten. `createAndOpenMarkdownFile` ist ein dünner Alias auf `createNewMarkdownFile` — das Öffnen als Tab passiert in Slatebase bereits als Seiteneffekt von `vault.create()`. `promptForFolderDeletion` spiegelt `promptForFileDeletion` für Ordner. `promptForFileRename` loggt eine Konsolenmeldung und lässt die Datei unangetastet (kein Rename-Dialog in Slatebase).

**Frontmatter-Parsing über echte `yaml`-Bibliothek** (`file-manager-shim.ts`, `metadata-parser.ts`, plus die global aufrufbaren `parseYaml`/`stringifyYaml` in `obsidian-api-extensions.ts`): Verschachtelte Objekte/Arrays in Frontmatter (nicht nur flache `key: value`-Paare und Inline-Arrays) werden korrekt geschrieben und gelesen — relevant für jedes Plugin mit strukturiertem Frontmatter, das über `processFrontMatter()` oder die globalen YAML-Funktionen liest/schreibt, z. B. **Day Planners** verschachtelte `planner: { log: [...] }`-Struktur.

### Kontextmenüs
`workspace.on('file-menu' | 'files-menu' | 'editor-menu' | 'url-menu', …)` wird real ausgelöst — eine zentrale `plugin-menu-bridge.ts` übersetzt die von Plugins befüllten `Menu`-Objekte in Slatebases Rechtsklick-Menüs auf praktisch jeder Oberfläche: Tab-Leiste (`file-menu`, Source `'tab-header'`, wie in echtem Obsidian), Datei-Explorer, Editor-Selektion (`editor-menu`), gerenderte Links in Live Preview und View Mode (`file-menu` für interne Wikilinks, `url-menu` für externe), Graph-Knoten und Suchergebnis-Dateigruppen (`file-menu`). `Menu`/`MenuItem` unterstützen Submenüs und Checkbox-Items vollständig, inklusive UI-Rendering (Hover/Klick/Pfeiltasten-Navigation für Submenüs, `aria-checked` für Checkbox-Items).

Die synthetischen `TFile`/`TFolder`-Objekte, die für diese Menü-Events gebaut werden (`plugin-event-bridge.ts`), haben einen korrekten Eltern-Ordner und das korrekte Prototype, sodass `file instanceof TFile` funktioniert — relevant z. B. für **Excalidraws** Tab-/Datei-Menü-Handler „New drawing".

### Embeds
`app.embedRegistry` ist eine echte, aber öffentlich undokumentierte Obsidian-Core-API (fehlt in `obsidian.d.ts`), über die Plugins eigene, nicht-Markdown-Embed-Typen für `![[datei.ext]]` registrieren — dieselbe Registry, über die Obsidians eigene Bild-/PDF-/Canvas-Embeds laufen. Implementiert: `registerExtension`/`registerExtensions`/`unregisterExtension`/`unregisterExtensions`/`isExtensionRegistered`/`getEmbedCreator`/`embedByExtension` (letzteres als live, direkt mutierbares Objekt — mindestens ein Plugin liest es als Property statt über `getEmbedCreator()`). Bekannte reale Nutzer: Supernote, tldraw, PDF++, drawio, canvas-cover-overlay, generische Helfer wie obsidian-dev-utils' `EmbedExtensionsComponent`.

**Kanban-spezifischer Fall — `embedByExtension.md` als Zugriffspfad auf eine private Obsidian-Klasse:** Kanban braucht für seine Inline-Karten-Editoren einen vollwertigen, eigenständigen CodeMirror-6-Editor — dafür gibt es in Obsidians öffentlicher Plugin-API kein Bauteil. Kanban umgeht das, indem es Obsidians eigenen `embedByExtension.md`-Eintrag (normalerweise zuständig für `![[note.md]]`-Einbettungen) direkt aufruft — nicht über `getEmbedCreator()` und nicht über den normalen `(context, file, subpath)`-Creator-Vertrag —, dann die Prototype-Kette der zurückgegebenen Instanz hochläuft und sich Obsidians private `MarkdownEditor`-Klasse daraus zieht, um sie zu subclassen. Das ist bereits in echtem Obsidian ein Zugriff auf nicht dafür vorgesehene Interna, kein von Obsidian bereitgestellter Erweiterungspunkt — es gibt also keine allgemeine, sauber nachbaubare Lösung dafür, nur die Nachbildung von genau dem, was Kanbans Code erwartet. Slatebase seedet deshalb `embedByExtension.md` bereits beim Modul-Load (`embed-registry.ts`, `seedKanbanMarkdownEmbed()`, guarded durch `isExtensionRegistered('md')` — ein echtes `registerExtension('md', ...)` eines Plugins überschreibt den Seed, genau wie es Kanbans Hack auch in echtem Obsidian bräche) mit einem `FakeEditor`, der eine echte CodeMirror-6-`EditorView` aufbaut, damit Kanbans `.set()`/`.get()`/`.cm.dispatch()`-Aufrufe funktionieren.

### Plugin-Verwaltung (`app.plugins`)
`getPlugin`, `plugins`, `enabledPlugins`, `manifests`, `loadManifests`, `requestSaveConfig`, `enablePluginAndSave`, `disablePluginAndSave`. `loadManifests()` fragt `manifest.json` für alle installierten Plugins neu vom Backend ab (derselbe Refresh, den der Vault-Start ohnehin macht). `requestSaveConfig()` wartet auf ausstehende Registry-Writes — die selbst schon bei jeder Status-/Permission-Änderung sofort ans Backend persistiert werden, hier also ein ehrlicher Flush statt eines Leerlauf-Stubs. `enablePluginAndSave`/`disablePluginAndSave` delegieren 1:1 an denselben Pfad wie der Settings-Seiten-Toggle — **inklusive** eines `window.location.reload()` nach dem Disable, aber nur wenn die deaktivierte Plugin-ID in `PLUGINS_REQUIRING_RELOAD_ON_DISABLE` (`plugin-context.ts`) steht — aktuell nur `obsidian-livesync`, das nach einem Unload nicht sauber innerhalb derselben Session neu starten kann (siehe Eintrag oben). Alle anderen Plugins werden ohne Reload direkt im laufenden Tab bereinigt (`unload()`/Sandbox-Cleanup). Löst die Bedingung aus, gilt sie für **jeden** Aufruf von `disablePluginAndSave`, unabhängig davon, welches Plugin es aufruft oder welche ID übergeben wird — es gibt keine Scope-Beschränkung auf das aufrufende Plugin selbst.

### Commands, Hotkeys & Core-Commands
`addCommand`/`removeCommand`, Command-ID-Namespacing (`<pluginId>:<commandId>`), Suche (case-insensitive, 50 Treffer-Limit). Obsidians eingebaute Commands (`editor:*`, `workspace:*`, `app:*`, `file-explorer:*`, `theme:*`) sind unter ihrer echten ID registriert, damit `executeCommandById()` funktioniert. Commands ohne Slatebase-Entsprechung existieren weiterhin, statt gar nicht zu existieren — sie sind im Spec als `unsupported: <Grund>` markiert und melden beim Ausführen als Toast, was fehlt, statt wirkungslos zu bleiben. `app.commands` und `app.hotkeyManager.customKeys` sind echte, iterierbare Registries (keine leeren Platzhalter).

#### Native Zwillinge in der Command Palette

Ein Teil der Core-Commands hat ein natives `slatebase:*`-Gegenstück, das der Command Palette als übersetzter, eigenständiger Eintrag angezeigt wird — der Core-Command selbst bleibt registriert (für `executeCommandById()`), wird aber aus der Palette-Liste gefiltert (`DUPLICATE_OF_NATIVE_COMMAND` in `CommandPaletteContainer.tsx`).

Wo das native Gegenstück nur eine Palette-Fassade ist — also selbst nur `commandRegistry.executeCommand('<core-id>')` aufruft (Fold-Operationen, Notiz-Composer, Canvas-Befehle, Gliederung/Backlinks/Tags/Lesezeichen öffnen) — funktioniert es unabhängig vom `obsidian-plugin-compat`-Feature-Toggle: `registerCoreEditorCommands`/`registerCoreAppCommands` registrieren unconditional in die Registry, der Toggle steuert nur die *Palette-Sichtbarkeit* der Compat-IDs selbst, nicht `CommandRegistry.executeCommand()`. Ohne diesen nativen Zwilling würde das Ausschalten von Plugin-Kompatibilität rein native Slatebase-Funktionen (z.B. Canvas-Export) unerreichbar machen, obwohl sie mit Fremd-Plugins nichts zu tun haben.

Die Filterung ist bedingungslos: ein Core-Command mit Eintrag in `DUPLICATE_OF_NATIVE_COMMAND` ist immer ausgeblendet, unabhängig davon, ob sein natives Gegenstück gerade selbst sichtbar ist (offener Editor-Tab, ausgewählter Vault, …). Das war früher anders — bei ausgeblendetem nativen Befehl erschien der Core-Command als Fallback. Das erwies sich als nutzlos: dieselbe Bedingung, die den nativen Befehl ausblendet, blockiert auch den Core-Command beim Ausführen (entweder weil sein `editorCallback` denselben aktiven Editor-Kontext braucht, oder weil der aufgerufene Handler dieselbe Vorbedingung selbst nochmal prüft) — der Fallback war also nie mehr als ein Klick ins Leere.

#### Core-Commands ohne Slatebase-Entsprechung

Diese Command-IDs sind registriert (damit `executeCommandById()` sie findet), führen aber keine Aktion aus. Statt still zu bleiben — was sich für den Benutzer nicht von einem Bug unterscheidet — zeigen sie einen Info-Toast mit dem Grund. Grund und Text stehen in `UNSUPPORTED_REASONS` (`frontend/src/plugins/compat/core-command-i18n.ts`), die Zuordnung im jeweiligen Spec als `unsupported: '<key>'`.

**Dauerhaft ausgeschlossen** (Architekturentscheidung, kein Bugfix-Kandidat):

| Command-ID(s) | `unsupported` | Grund |
|---|---|---|
| `workspace:split-vertical`, `workspace:split-horizontal`, `workspace:toggle-stacked-tabs`, `focus-top`, `focus-bottom`, `focus-left`, `focus-right` | `tab-layout` | Flaches Tab-System ohne Split-Baum, Tab-Gruppen oder gestapelte Tabs — dieselbe Entscheidung wie bei [Split-Pane-/Multi-Window-Layout](#bekannte-bewusst-nicht-behobene-lücken). |
| `workspace:new-window`, `workspace:close-window`, `workspace:move-to-new-window`, `workspace:open-in-new-window`, `window:toggle-always-on-top` | `native-windows` | Slatebase läuft in einem Browser-Tab und verwaltet keine eigenen Fenster. |
| `workspace:new-tab` | `blank-tab` | Ein leerer Tab ohne Datei ist nicht vorgesehen: Tabs entstehen beim Öffnen einer Datei. |
| `bases:add-item`, `bases:add-view`, `bases:change-view`, `bases:copy-table`, `bases:insert`, `bases:new-file` | `bases` | Bases (Formel-Engine über Ordner-Tabellen) liegt außerhalb des Scopes dieses Layers, siehe [Bekannte, bewusst nicht behobene Lücken](#bekannte-bewusst-nicht-behobene-lücken). |
| `open-with-default-app:open`, `open-with-default-app:show` | `desktop-shell` | Desktop-only-Konzepte („in nativer App öffnen", „im System-Explorer anzeigen") — Dateien liegen serverseitig, es gibt kein lokales Dateisystem. |
| `download-attachments` | `attachments` | Es gibt keinen automatischen Download externer Bild-Links in den Vault — Anhänge kommen per Drag & Drop, Einfügen aus der Zwischenablage oder „Anhang einfügen" hinein. |
| `app:open-help` | `in-app-help` | Die Anleitung steht im Welcome-Vault statt in einer eigenen Hilfe-Ansicht. |
| `app:toggle-default-new-pane-mode` | `new-tab-defaults` | Neue Tabs öffnen immer im zuletzt genutzten Modus; es gibt keine Split-Pane-Voreinstellung zum Umschalten. |

**Geplant, aber noch nicht umgesetzt** (im Unterschied zur Liste oben *nicht* dauerhaft ausgeschlossen, sondern bewusst auf später verschoben):

| Command-ID | `unsupported` | Was dafür fehlt |
|---|---|---|
| `workspace:copy-url` | `url-scheme` | Es gibt (noch) kein URL-/Routing-Schema, das Vault+Datei auf einen wiederverwendbaren Link abbildet; Deep-Linking wäre ein eigenständiges Feature (Router, URL-Parsing beim Laden), keine reine Command-Verdrahtung. |
| `graph:animate` | `graph-animation` | Der Graph trägt heute keine Erstellungsdatum-Metadaten je Knoten, und ein echter Zeitraffer bräuchte zusätzlich eine Scrubber-/Abspiel-UI. |

**Voll implementiert, obwohl Obsidian dort mehr UI hat:** `footnotes:open` führt zur ersten Fußnoten-Definition der Notiz (Editor-Cursor bzw. gerenderte Fußnotenliste) — ein eigenes Seitenpanel gibt es nicht, die Fußnoten stehen in der Notiz selbst. `editor:attach-file` öffnet den nativen Datei-Dialog des Browsers, lädt die Auswahl in das vault-konfigurierte Anhänge-Verzeichnis (`attachmentsDirectory`, Einstellungen → Vault-Konfiguration; Default: gleicher Ordner wie die Notiz) hoch und fügt an der Cursorposition je Datei einen Wikilink (bzw. Embed für Bilder/PDFs) ein — denselben Weg nutzen Drag & Drop und der native Command `slatebase:attach-file` (alle drei lösen dasselbe `slatebase:editor-command`-Fensterevent mit `action: 'attachFile'` aus, siehe `EditMode.tsx`). `canvas:export-as-image` rendert die *live* DOM des sichtbaren Canvas — nicht eine zweite, aus dem Dokumentmodell abgeleitete Repräsentation — über `html-to-image` (`toBlob`) auf ein temporär auf 1:1-Zoom mit allen Knoten gefittetes Viewport und lädt das Ergebnis als PNG herunter (`CanvasView.tsx`s `exportAsImage()` plus der `ActiveCanvasController`-Bridge-Eintrag). Bekannte Lücke: `LinkNodeRenderer`s iframe-Vorschauen bleiben im Export leer, da Cross-Origin-iframe-Inhalte grundsätzlich nicht in ein `<canvas>` gelesen werden können.

### Events, Scope & Keymap
`on`/`off`/`trigger`/`offref` mit garantierter Registrierungsreihenfolge und Exception-Isolation pro Callback. `Scope`/`Keymap` sind echt (nicht nur Attrappen): `app.keymap.pushScope`/`popScope` verwalten einen fenster-globalen Scope-Stack, `Scope.handleKey()` matcht Modifier+Taste gegen registrierte Handler.

### Settings & Persistenz
`loadData`/`saveData` (Round-Trip, max. 1 MB, pro Plugin + Vault isoliert). Deklarative Settings-API (`getSettingDefinitions()`, ab Obsidian 1.13) wird automatisch gerendert (`group`, `list`, `page`, alle Standard-Controls). `Plugin.onExternalSettingsChange` ist über SSE End-to-End verdrahtet — eine Einstellungsänderung in einem Tab/Gerät erreicht die anderen live.

### CSS
Plugin-`styles.css` (max. 512 KB) wird beim Aktivieren injiziert und beim Deaktivieren vollständig entfernt, alle Selektoren automatisch auf `[data-plugin-id="…"]` gescoped (Descendant- und Self-Form).

`theme-dark`/`theme-light` und die Plattform-Klassen (`is-mobile`, `is-phone`, `is-tablet`, `mod-macos`, `mod-windows`, `mod-linux`, `mod-ios`, `mod-android`) werden auf `document.body` synchronisiert — sowohl für Runtime-Checks (`document.body.classList.contains('theme-dark')`) als auch für Plugin-CSS. Beim Scoping bleiben diese Host-Klassen **vor** dem Plugin-Scope stehen statt in ihn hineingefaltet zu werden, denn sie sitzen auf `<body>` und nicht auf dem Plugin-Element:

```css
/* Plugin schreibt */   .theme-dark .panel { … }
/* injiziert wird */    body.theme-dark [data-plugin-id="x"].panel,
                        body.theme-dark [data-plugin-id="x"] .panel { … }
```

Der Prefix wird auf **jede** erzeugte Alternative angewandt — sonst bliebe die Descendant-Form ungeschützt und eine Dark-Mode-Regel griffe auch im Light-Mode. Quelle der Klassenliste ist `OBSIDIAN_HOST_BODY_CLASSES` in `body-classes.ts`, dieselbe, die die Klassen setzt; `css-injector.ts` importiert sie, damit beide nicht auseinanderlaufen.

Struktur-CSS für die Obsidian-Komponentenklassen, die die Shims rendern (`.menu`, `.suggestion-item`, `.prompt`, `.notice`, `.svg-icon`, `.modal-title`, …), liegt in `obsidian-components.css` und ist durchgehend in den Obsidian-Variablen aus `obsidian-compat.css` (605 Stück) formuliert, folgt also automatisch dem aktiven Theme.

Wo Slatebase eigene Klassennamen hat, tragen die Elemente zusätzlich den Obsidian-Namen, damit Plugin-CSS trifft: Ribbon-Buttons `side-dock-ribbon-action`, Status-Bar-Items `status-bar-item`, Dateibaum-Zeilen `nav-file-title`/`nav-folder-title`, Tooltips `tooltip`. Die eigentliche Gestaltung bleibt bei den Slatebase-eigenen Klassen.

**Modal-Größe & `SuggestModal`/`FuzzySuggestModal`:** Der generische `Modal`-Shim (`install-globals.ts`) setzt beim Öffnen die Fenstergröße per Inline-Style (`max-width: 840px; max-height: 85vh`) — bewusst großzügig bemessen, da Plugins damit alles von schmalen Bestätigungsdialogen bis zu grid-lastigen Pickern (z. B. Iconizes Icon-Auswahl) abdecken. `SuggestModal` erweitert diesen `Modal`-Shim und setzt zusätzlich `modalEl.classList.add('prompt')` — real Obsidians Konvention für Suchmodals, auf die Plugin-CSS routinemäßig scoped (z. B. Iconizes `.prompt .iconize-subheadline` für die „Recently used icons"-Zwischenüberschrift samt Grid-Spannen über alle Spalten; ohne die Klasse griff die Regel nie, und die Überschrift wurde von CSS Grid ungespannt auf die Höhe einer Icon-Zeile gestreckt). Die Ergebnisliste (`resultContainerEl`) scrollt innerhalb von `max-height: 65vh`.

**Heading-Skala in Plugin-DOM:** `--h1-size` bis `--h6-size` (u. a. Farbe/Gewicht) aus `obsidian-compat.css` werden über `[data-plugin-id] h1`..`h6` auf von Plugins selbst gerendertes `<h1>`-`<h6>` angewendet — bewusst auf Plugin-DOM beschränkt (jedes Plugin-Modal/-View trägt `data-plugin-id`), damit Slatebases eigene, bereits über spezifischere Selektoren gestylte Headings unangetastet bleiben.

### Sicherheit / Sandbox
Deny-by-default-Permissions (Netzwerk, Dateisystem-Schreibzugriff, DOM-Manipulation — alle `false` bei Neuinstallation). Cross-Origin-Requests laufen über einen Backend-Proxy mit Domain-Allowlist. Bundle-Integritätsprüfung lehnt `eval(`, `new Function(`, `document.write(` ab. Main-Thread-Blockierung >5s deaktiviert ein Plugin automatisch. Eine `ErrorBoundary` um den Plugin-Provider verhindert, dass ein Plugin-Fehler wie ein App-Absturz aussieht. `sanitizeHTMLToDom()` entfernt `<script>`-Tags, Inline-Event-Handler-Attribute (`onclick=`, `onerror=`, …), `javascript:`/`vbscript:`-URLs und gefährliche `data:`-URLs (`text/html`, `image/svg+xml`) aus `href`/`src`/`action`/`formaction`. `SecretStorage`/`SecretComponent`-Werte liegen serverseitig AES-256-GCM-verschlüsselt (`backend/src/plugin/secret-key-manager.ts`, `secret-store.ts`) unter `data/plugins/<vaultId>/<pluginId>/secrets.json`; der List-Endpunkt liefert nur IDs, nie Werte.

### Sonstiges
Globale Obsidian-Prototype-Erweiterungen (`Array.prototype.remove/first/last`, `Element.prototype.find/findAll`, `String.prototype.contains`, `Math.clamp/square`, u.a.), vollständige Lucide-Icon-Auflösung (`setIcon`/`getIcon`, kein hartes `null` für gelistete IDs), `requestUrl()`, CodeMirror-6-Extensions (echte `@codemirror/*`-Module, kein Stub), `SuggestModal`/`FuzzySuggestModal`.

**`@codemirror/lint` wird nicht nur an Plugins durchgereicht (`window.__codemirrorLint`), sondern auch vom Kern selbst genutzt** — die eingebaute Rechtschreibprüfung zeichnet ihre Unterringelungen darüber (`editor/spellcheck/spellcheck-extension.ts`). Für Plugins ändert das nichts: `linter()`-Extensions koexistieren, CM6 führt die Diagnostics aller Quellen zusammen. Zwei Details, falls ein Plugin eigene Lint-Ergebnisse anzeigt:

- Slatebases Diagnostics tragen `source: 'slatebase-spellcheck'` und die Marker-Klasse `cm-spellError`; sie sind über `forEachDiagnostic()` von Plugin-Diagnostics unterscheidbar.
- Die Rechtschreibprüfung unterdrückt **nur ihre eigenen** Hover-Tooltips (`tooltipFilter` filtert nach `source`) — Tooltips fremder Linter bleiben unangetastet.

Der Editor setzt `contentDOM.spellcheck = false`, weil er selbst prüft. Ein Plugin, das die native Browser-Prüfung erwartet, bekommt sie im Haupteditor nicht — in eigenen `<textarea>`/`contenteditable`-Elementen eines Plugins gilt das nicht, die bleiben unberührt.

**Icons synchron wie in echtem Obsidian:** Der komplette eingebaute Icon-Satz (Lucide, per `dynamicIconImports`) wird einmal pro Session parallel eager aufgelöst (`preloadAllBuiltInIcons()`, `lucide-icons.ts`), bevor das erste Plugin-Bundle einer Session ausgewertet wird — nicht erst bei Bedarf pro Icon. Das hält den Haupt-Bundle klein (weiterhin dynamische Imports statt statischem Einbinden aller ~2000 Icons), verhält sich aber ab dem ersten Plugin-Load nach außen synchron: Auch Plugins, die Icons einmalig beim Modul-Load in einen eingefrorenen React/vDOM-Baum einbacken (z. B. Excalidraws `ICONS = { Foo: getIconAsJSX('image-down'), ... }`), bekommen bereits aufgelöste Icon-Daten statt einer leeren Hülle.

`Notice` baut Obsidians echten DOM-Baum (`containerEl > noticeEl > messageEl`) und übergibt `noticeEl` an den Toast-Stack, der genau dieses Element einhängt. Ein Plugin, das nach dem Konstruktor in `messageEl` schreibt (fortlaufende Fortschrittszeile, Spinner, Link) oder ein `DocumentFragment` übergibt, sieht das entsprechend auf dem Bildschirm.

---

## Was wird nicht unterstützt

Zwei grundsätzlich verschiedene Kategorien, die auch die Kompatibilitäts-Badges auseinanderhalten (siehe [Kompatibilitäts-Klassifizierung](#kompatibilitäts-klassifizierung)):

- 🔴 **Rot** = das Plugin lässt sich vermutlich nicht aktivieren und/oder erzeugt echte Laufzeit-Fehler (`console.error`/uncaught exceptions) — die Anwendung als Ganzes stürzt dabei nie ab.
- 🟡 **Gelb** = bewusst nicht implementierte Funktionalität, weil es in Slatebase kein Äquivalent gibt. Jeder solche Aufruf liefert sicher einen No-Op zurück, loggt beim ersten Zugriff eine `console.warn` (nie stillschweigend) und blockiert weder die Aktivierung noch die Kernfunktion des Plugins insgesamt.

### 🔴 Rot

- **Desktop-only-Plugins** (`manifest.json`: `isDesktopOnly: true`): Nutzen Node.js/Electron-APIs (Dateisystem, Kindprozesse, native Module), die im Browser nicht existieren. Der Community-Plugin-Store blendet für diese Plugins den Install-Button aus. Ein manueller ZIP-Upload ist technisch nicht blockiert, das Plugin wird aber als „Nicht unterstützt" markiert und wird in aller Regel beim Laden fehlschlagen. 11 der Top-100-Plugins sind betroffen.

### 🟡 Gelb

- **Echte Node.js-Builtins ohne echten Browser-Ersatz** (`fs`, `child_process`, `net`, `electron`, `stream`, …, außer `path`/`buffer` — die haben echte Browser-Shims): Schon die bloße Referenz im Bundle (`require('fs')`/`import`) färbt das Badge gelb, unabhängig davon, ob ein Mitglied davon zur Laufzeit tatsächlich aufgerufen wird — die statische Analyse kann "nur importiert" nicht zuverlässig von "wirklich gebraucht" unterscheiden (**Praxisbeispiel:** Iconize referenziert `fs`/`stream` über eine gebündelte ZIP-Bibliothek fürs Icon-Pack-Herunterladen und ruft davon zur Laufzeit tatsächlich `readFile`/`readFileSync` auf, obwohl das Plugin nicht `isDesktopOnly` ist). Ruft der Plugin-Code tatsächlich eine Methode auf einem solchen Modul auf, liefert der Sandbox-`require()`-Shim (`plugin-loader.ts`) einen warnenden No-Op statt eines rohen `{}`, an dem der Methodenaufruf sonst einen ungefangenen `TypeError` werfen würde — das ist der Absturzschutz zur Laufzeit; das Gelb-Badge ist das Vorab-Warnsignal beim Installieren.
- **Echtes Split-Pane-/Fenster-Layout**: Slatebase hat ein flaches Tab-System, keinen Baum aus Splits. `createLeafBySplit`/`splitActiveLeaf` erzeugen einen neuen Tab statt eines echten Splits; `getLayout()`/`rootSplit` etc. sind Stubs ohne reale Baumstruktur. Plugins, die den Split-Baum aktiv inspizieren oder umbauen, funktionieren nicht wie in Obsidian. (Zählt im Analyzer als *unterstützt/grün*, siehe „Graceful Degradation" oben — konsequent gelogged, aber kein Gelb-Badge, weil das Ersatzverhalten offensichtlich und harmlos ist.)
- **Popout-Fenster / Multi-Window**: `openPopoutLeaf`/`moveLeafToPopout` öffnen kein separates Browser-Fenster — sie fallen auf einen normalen Tab zurück.
- **`app.plugins.installPlugin`**: Lädt in echtem Obsidian ein Plugin direkt von einem beliebigen GitHub-Repo herunter und führt es aus (die Grundlage von BRAT). Bewusst **nicht** emuliert, aus Sicherheitsgründen statt technischer Unmöglichkeit: Slatebases echter Install-Pfad (`apiClient.installFromStore`) läuft absichtlich backend-vermittelt über einen kuratierten Plugin-Store. Ein plugin-aufrufbares `installPlugin` würde diesen Trust-Boundary umgehen — jedes installierte Plugin könnte dann selbständig und ohne Nutzerbestätigung weitere Plugins nachladen.
- **`app.plugins.getPluginFolder`**: nicht emuliert — Slatebase hat kein Dateisystem-Konzept pro Plugin (`.obsidian/plugins/<id>/`), ein zurückgegebener Pfad wäre reine Fiktion.
- **`app.plugins.checkForUpdates`** und **`app.plugins.updates`** (Update-Status-Registry): nicht emuliert — Slatebase hat einen eigenen Update-Mechanismus im Plugin-Store.
- **`vault.setConfig()`**: Schreibzugriff auf Obsidians App-Einstellungen (z. B. `readableLineLength`) ist nicht emuliert, nur der Lesezugriff (`getConfig()`).
- **Drag&Drop-Reordering von Leaves**: nicht emuliert — Slatebase hat kein Konzept einer manuell umsortierbaren Tab-Reihenfolge innerhalb einer Split-Ebene.
- **`metadataCache.inProgressTaskCount`**, **`metadataCache.onCleanCache`**, **`metadataCache.initialized`**: nicht emuliert — Slatebase indiziert synchron on-demand, es gibt keinen asynchronen Indexierungsfortschritt zu melden.
- **Obsidians natives Link-Kontextmenü-Hook** und **`fileManager.iterateAllRefs()`** (Referenz-Iteration über den ganzen Vault): nicht emuliert.
- **`workspace.getGroupLeaves`**: nicht emuliert — Slatebase hat kein Leaf-Gruppierungskonzept.
- **`getUnpinnedLeaf()` sucht kein echtes ungepinntes Leaf**: Tabs haben seit `workspace:toggle-pin` ein echtes `pinned`-Flag, aber `getUnpinnedLeaf()` fragt es nicht ab und erstellt weiterhin immer ein neues Leaf statt ein bestehendes ungepinntes wiederzuverwenden (wie echtes Obsidian).
- **Deferred Leaves** (`WorkspaceLeaf.isDeferred`/`loadIfDeferred`, seit Obsidian 1.7.2): immer `false`/No-Op, weil `setViewState()` in Slatebase Views immer synchron-eager erstellt — es gibt nie einen „deferred" Zustand zu melden.
- **Obsidian Sync / Publish**: proprietäre Obsidian-Dienste, kein Bestandteil der Plugin-API-Emulation.
- **Vim-Keybindings im Editor**: Slatebase hat keine eigene Vim-Keymap-Engine (`@replit/codemirror-vim`). `window.CodeMirrorAdapter.Vim` existiert nur als No-Op-Stub (`defineAction`/`handleEx`/`enterInsertMode`/`mapCommand`), damit Plugins, die optional daran andocken (z. B. Outliner, s. o.), nicht mit `console.error` abbrechen — echte Vim-Normalmodus-Befehle laufen dadurch aber nicht. Jeder Aufruf loggt einmal pro Methode ein `console.debug`.
- **Weitere bewusste No-Ops, alle mit Log** — es gibt in der Compat-Schicht keinen stillen Stub mehr:
  - `window.CodeMirror.*` (CM5-Legacy: `defineMode`/`defineMIME`/`defineExtension`/`defineOption`/`registerHelper`/`registerGlobalHelper`) → `console.debug`. Slatebases Editor ist CM6; Plugins, die CM5 ansprechen, nutzen eine von Obsidian selbst als veraltet geführte API und liefern in aller Regel zusätzlich einen CM6-Pfad.
  - `MarkdownView.previewMode.set/clear/rerender/applyScroll` → `console.warn`. Reading-Mode läuft über Slatebases eigene React-Pipeline, nicht über eine Obsidian-`MarkdownPreviewView`.
  - `app.foldManager.save/load` → `console.debug`. Fold-Zustand wird nicht über Sessions hinweg gespeichert.
  - `HTMLElement.onNodeInserted` → `console.warn`. Feuert nie; Plugin-Code, der darauf wartet (verzögertes Messen/Layout), läuft dadurch nicht.
  - `HTMLElement.onWindowMigrated` → `console.debug`. Ein Single-Window-App hat nichts zu melden.
  - `hotkeyManager.setHotkeys`/`removeHotkeys` → `console.warn`. Slatebase hat keine Hotkey-Anpassung, in die geschrieben werden könnte.
  - `loadPrism` und `loadPdfJs` → `console.warn` und `null`. Slatebase liefert weder Prism (Syntax-Highlighting läuft über `highlight.js`) noch PDF.js mit. `loadMathJax`/`renderMath`/`finishRenderMath` sind dagegen **echt** — sie laufen über das mitgelieferte KaTeX (`components/katex-loader.ts`).
  - Der Bootstrap-`window.app.commands` (nur gültig bis der `PluginProvider` den echten, von der `CommandRegistry` gedeckten Manager einsetzt) warnt und liefert `false` statt still nichts zu tun, wenn ein Plugin schon während der Bundle-Auswertung Kommandos ausführen will.
- **`fallback-shims.ts`s Sicherheitsnetz-Klassen** (u. a. `FallbackColorComponent`, `FallbackSearchComponent`, `FallbackMomentFormatComponent`, `FallbackAbstractInputSuggest`): registrieren sich unter dem echten Obsidian-Klassennamen (`ColorComponent`, `SearchComponent`, …) nur, falls dieser Name noch nicht belegt ist (First-Writer-wins, `set()` in `fallback-shims.ts`). Da `registerObsidianApiExtensions()`/`install-globals.ts` die echten, vollständigen Implementierungen (siehe [Klassen-Abgleich](#klassen-abgleich-gegen-die-offizielle-obsidian-api-obsidiandts)) immer zuerst registrieren, greifen diese Fallbacks in der Praxis nie — sie sind reine Absturzverhinderung für den unwahrscheinlichen Fall, dass der reguläre Registrierungspfad einmal nicht läuft. Jeder erste lesende Zugriff auf einen tatsächlich nicht emulierten `obsidian.<Name>` loggt eine `console.warn` und ist im `window.__slatebasePluginApiGaps()`-Register sichtbar — nie ein stiller Stub. Ebenso jeder nicht emulierte `vault.adapter.*`-Zugriff (z. B. `copy`, `getResourcePath`, `process` — Obsidians volles `DataAdapter`-Interface).

**Geplant, aber noch nicht implementiert:**
- Server-seitige Ausführung von Desktop-only-Plugins in einer Node.js-VM-Sandbox steht auf der Roadmap, würde aber Dateisystem-/Prozess-APIs serverseitig statt im Browser bereitstellen — bis dahin bleiben Desktop-only-Plugins vollständig außen vor.
- **`internalPlugins.plugins.canvas`-Anbindung an Slatebases native `.canvas`-Ansicht:** Aktuell reiner Absturz-Schutz (siehe [Embeds](#embeds) sowie Zeile #1 in [Top 100](#top-100-community-plugins-nach-downloads)) — `createFileNode()`/`removeNode()`/`setReadonly()` sind No-Ops, es wird nichts wirklich eingebettet. Eine echte Anbindung ist machbar (Slatebases Canvas-Reducer hat mit `ADD_NODE`/`DELETE_NODES` und dem `FileNode`-Typ bereits die nötigen Bausteine, und `.canvas`-Dateien laufen bereits im selben Tab-/Leaf-System wie alles andere), erfordert aber eine neue Leaf→CanvasView-Bridge (analog zu `WorkspaceShim.setEditorTextarea()`/`setOnOpenFile()`), eine neue `SET_READONLY`-Reducer-Action und akzeptiert einen bewussten Kompromiss: Slatebases eigener `FileNodeRenderer` übernimmt das Rendern, nicht das Plugin selbst — ein Plugin-`.render()`-Aufruf auf dem zurückgegebenen `containerEl` hätte damit keine Wirkung mehr. Mittlerer Aufwand (Tage, kein Nachmittags-Fix), aber kein neues Subsystem wie die VM-Sandbox oben, nur eine Brücke zwischen zwei bereits vorhandenen. Relevant nicht nur für Excalidraw, sondern besonders für **Advanced Canvas** (#35, siehe [Top 100](#top-100-community-plugins-nach-downloads)), dessen gesamter Zweck die Manipulation von Canvas-Internas ist.

---

## Klassen-Abgleich gegen die offizielle Obsidian-API (`obsidian.d.ts`)

Jede öffentliche Methode/Property von 62 API-Klassen (288 Mitglieder insgesamt) wurde mechanisch gegen das offizielle, MIT-lizenzierte npm-Paket `obsidian` (`obsidian.d.ts`, Version 1.13.1) geprüft, mit tatsächlichem Lesen der jeweiligen Shim-Implementierung.

**Ergebnis:** 139 vollständig (48 %), 99 Stub (34 %), 50 fehlt komplett (18 %). Klassen wie `Modal`, `Menu`, `Setting` und die meisten UI-Komponenten (`ColorComponent`, `ButtonComponent`, …) sind plain JS-Klassen ohne das Proxy-Sicherheitsnetz, das `App`/`Vault`/`Workspace`/`MetadataCache`/`FileManager` schützt — eine fehlende Methode wirft dort einen ungefangenen `TypeError` statt eines sicheren No-Ops. Die Tabelle unten markiert jede so verbleibende Absturzstelle explizit mit **crasht**.

**Wichtig zur Einordnung:** „Vollständig" heißt hier „verhält sich für die gängigen Anwendungsfälle wie in echtem Obsidian", nicht zwingend 1:1-Übereinstimmung jeder einzelnen Methode — eine als „Vollständig" markierte Klasse kann trotzdem einzelne fehlende Randmethoden haben (in der Begründungsspalte vermerkt). Das ist bewusst gröber als die Methoden-Level-Tabelle, damit diese Übersicht lesbar bleibt.

| Klasse | Status | Begründung |
|---|---|---|
| `Vault` | ✅ Vollständig | Alle Kern-CRUD/Read/Events real; `tryTrigger()` (deprecated) fehlt, landet im Proxy-Fallback. |
| `App` | ✅ Vollständig | Vault/Workspace/MetadataCache/FileManager/Scope/Commands echt verdrahtet; `lastEvent`, `renderContext`, `isDarkMode()` fehlen (Proxy-Fallback). |
| `Workspace` | ✅ Vollständig | Leaf-API/Events/Tabs real; Split-Baum (`getLayout`/`rootSplit`/Ribbons) sind dokumentierte Stubs mit korrekter `instanceof`-Identität (`WorkspaceSplit`/`WorkspaceRibbon`, s. u.). `file-menu`/`files-menu`/`editor-menu`/`url-menu` werden über `plugin-menu-bridge.ts` echt ausgelöst (siehe [Kontextmenüs](#kontextmenüs)); `css-change`/`quit` bleiben registrierbar, aber nirgends ausgelöst. `activeLeaf instanceof WorkspaceLeaf` funktioniert (s. u.). |
| `MetadataCache` | ✅ Vollständig | `getFileCache()`/`getCache()` invalidieren über einen `onFileWrite`-Hook auch bei direktem `vault.modify()`/`create()`/`processFrontMatter()` außerhalb eines offenen Editor-Tabs, nicht nur über die Tab-Diff-Heuristik. |
| `FileManager` | ✅ Vollständig | `renameFile()` aktualisiert automatisch alle Wikilinks anderer Dateien, die auf den alten Pfad zeigen — läuft serverseitig im selben `moveContent`/`renameContent`-Endpunkt, den `vault.rename()` aufruft, unabhängig vom Auslöser (UI, Plugin, REST); siehe `.kiro/specs/graph-polish-link-integrity/`. `processFrontMatter()` refresht den MetadataCache nicht mit; `generateMarkdownLink()` ignoriert `sourcePath` (immer absoluter Pfad). |
| `Plugin` | ✅ Vollständig | Lifecycle, `addCommand`/`addSettingTab`/`registerView`/`registerEditorExtension`/`loadData`/`saveData` real; `registerBasesView`/`registerObsidianProtocolHandler`/`registerCliHandler` bewusste No-Ops; `registerEditorSuggest` bridged sowohl CM6-`provider` als auch Obsidians natives `onTrigger`/`getSuggestions`/`renderSuggestion`/`selectSuggestion`-Interface (volles Popover mit Keyboard-Navigation). |
| `PluginSettingTab` / `SettingTab` | ✅ Vollständig | `display()`-Lifecycle real; `refreshDomState()` fällt auf einen vollen Re-Render statt eines leichteren Partial-Refresh zurück (korrekt, nur teurer); `icon`/`settingItems` fehlen (Proxy-Fallback). |
| `Setting` | ✅ Vollständig | Fluent-Builder mit allen `add*`-Methoden real, inkl. `setVisibility()`, `errorEl`/`setErrorMessage()` (seit 1.13.0) und `setTooltip()` (echte aria-label-Mechanik). Dazu die drei Zeilen-APIs aus Obsidian 1.13, die in der veröffentlichten `obsidian.d.ts` fehlen und die Obsidians eigene Plugins sich selbst deklarieren: `setNavigable()` (ganze Zeile klickbar + Chevron), `setAction()` (Zeile als Button) und `setIcon()` (führendes Icon, `null` entfernt es). |
| `SettingGroup` | ✅ Vollständig | Echtes Header/List-DOM; `addExtraButton()` teilt den `setIcon()`-No-Op mit `Setting.addExtraButton`. |
| `SettingPage` | ✅ Vollständig | DOM-Grundgerüst + `hide()` räumt `containerEl` ab, `title`-Setter synchronisiert `titlebarEl`, `makeExtendable` für ES5-Downlevel-Bundles. Sub-Page-Navigation läuft über `declarative-settings-renderer.ts` (`openSubPage()`). |
| `Component` | ✅ Vollständig | Vollständiger Lifecycle; `registerEvent()` trackt nur Refs von Slatebase-eigenen Emittern. |
| `Events` | ✅ Vollständig | `on`/`off`/`trigger` real; `offref()` deregistriert wirklich (über eine `WeakMap<EventRef, EventSystem>`, damit der Ref kein internes Feld tragen muss, das echtes Obsidian nicht hat) und gibt `false` zurück, wenn der Ref nicht von einem EventSystem stammt; `tryTrigger()` ist ein No-Op. |
| `TAbstractFile` / `TFile` / `TFolder` | ✅ Vollständig | Prototype-Kette für `instanceof` real. `TFile.stat.ctime`/`mtime` kommen vom Backend-Verzeichnis-Scan (echte `fs.stat()`-Zeitstempel); nur für lokal-synthetisierte Knoten (gerade erstellte/umbenannte Datei vor dem nächsten Tree-Refresh) fällt es auf „jetzt" zurück. |
| `FileSystemAdapter` (`DataAdapter`) | ✅ Vollständig | Kernmethoden (`exists`/`read`/`write`/`list`/`stat`/`remove`/`rename`) echt ans Backend angebunden; `insensitive` liefert `false` (Slatebase-Backend-Storage ist case-sensitive); erweiterte Methoden (`copy`, `process`, `getResourcePath`, …) proxy-sicher abgefangen. |
| `Editor` | ✅ Vollständig | Fast komplettes CodeMirror-Interface (~40 Methoden geprüft); `exec()` deckt alle 17 `EditorCommandName`-Werte ab, inklusive `toggleFold`/`foldAll`/`unfoldAll` (dieselbe Fold-Engine wie die `fold-*`-Core-Commands, siehe `editor/folding.ts`); `undo`/`redo` ohne CM6 wirkungslos. `containerEl`-Getter sowie `coordsAtPos()`/`cursorCoords()` (CM5-Legacy-Form) sind reale, in `obsidian.d.ts` nicht dokumentierte Member, die **Editing Toolbar** zur Positionierung seiner schwebenden Auswahl-Toolbar aufruft. |
| `EditorSuggest` | ✅ Vollständig | Obsidians natives `onTrigger`/`getSuggestions`/`renderSuggestion`/`selectSuggestion`-Interface wird über einen CM6-ViewPlugin bei jeder Cursor-Bewegung getriggert. Positioniertes Dropdown-Popover mit Keyboard-Navigation (↑↓ Enter Tab Esc). `setInstructions()` ist ein bewusster kosmetischer No-Op (Footer-Hints werden nicht angezeigt). Async-`getSuggestions` via Generation-Guard stale-sicher; ein erzwungener Microtask-Yield vor dem Popover-Rendering verhindert `"Reading the editor layout isn't allowed during an update"` bei synchronen `getSuggestions()`-Implementierungen (der Aufruf läuft aus `ViewPlugin.update()` heraus). |
| `MarkdownView` | ✅ Vollständig | Echte Klassenkette `Component → View → ItemView → FileView → EditableFileView → TextFileView → MarkdownView`. `instanceof TextFileView`/`EditableFileView` beide `true`. `view.data` geerbt, `requestSave()` debounced (geerbt), `save()` geerbt. `previewMode` ist eine echte lazy `MarkdownPreviewView`-Instanz, `currentMode` eine lazy `MarkdownEditView`-Instanz. |
| `MarkdownEditView` | ✅ Vollständig | Implementiert `MarkdownSubView` + `HoverParent` + `MarkdownFileInfo`. Methoden: `get()`/`set()`/`clear()`/`getScroll()`/`applyScroll()`/`getSelection()`. Delegiert an den live CM6-Editor. Lazy instanziiert als `MarkdownView.currentMode`. |
| `MarkdownPreviewView` | ✅ Vollständig | Extends `MarkdownRenderer` (→ `MarkdownRenderChild` → `Component`). `MarkdownSubView`-Interface vollständig (`get`/`set`/`clear`/`getScroll`/`applyScroll`/`rerender`). Statische `render()`/`renderMarkdown()` delegieren an den echten Renderer. `rerender()` ist ein No-Op (kein separater Reading-Mode-Renderer). Lazy instanziiert als `MarkdownView.previewMode`. |
| `MarkdownRenderer` | ✅ Vollständig | Instanziierbare Klasse (extends `MarkdownRenderChild`, implements `HoverParent`). Instanz-Properties `app`/`hoverPopover`/`file`. Statische Methoden `render()`/`renderMarkdown()` nutzen die echte unified/remark-Pipeline. `instanceof MarkdownRenderChild` funktioniert. |
| `MarkdownRenderChild` | ✅ Vollständig | Erbt vollen Component-Lifecycle. |
| `MarkdownPreviewRenderer` | ✅ Vollständig | `registerPostProcessor()` delegiert an die echte `code-block-processor-registry` (mit Plugin-ID aus dem Ausführungskontext). Registrierte Post-Processors werden beim Rendering ausgeführt. |
| `View` / `ItemView` / `FileView` | ✅ Vollständig | `scope` (auf `View`) fehlt nirgends gesetzt; `onResize()`/`onPaneMenu()` No-Ops. |
| `TextFileView` | ✅ Vollständig | Echtes Debounced-Save; siehe Vererbungslücke bei `MarkdownView` oben. |
| `EditableFileView` | ✅ Vollständig | Echte leere Zwischenklasse (`extends FileView`), korrekte Position in der Kette `FileView → EditableFileView → TextFileView`. `instanceof` funktioniert. |
| `WorkspaceLeaf` | ✅ Vollständig | `window.obsidian.WorkspaceLeaf` ist dieselbe Klasse wie die echten Leaf-Objekte aus `view-registry.ts` — `instanceof`-Check funktioniert für jedes reale Leaf (`activeLeaf`, `getLeaf()`, `getRightLeaf()`, …). |
| `WorkspaceSplit`/`WorkspaceRibbon` | ✅ Vollständig, mit Einschränkung | Echte, gemeinsam genutzte Klassen (`view-registry.ts`) für `WorkspaceShim`s `rootSplit`/`leftSplit`/`rightSplit`/`leftRibbon`/`rightRibbon` — `instanceof`-Check funktioniert. `leftRibbon`/`rightRibbon` sind echt verdrahtet: `hide()`/`show()`/`toggle()`/`collapsed` steuern bzw. lesen Slatebases Werkzeugleiste (`SidebarToolbar`), und zwar die Ribbon-Seite, auf der die Leiste gerade angedockt ist (`toolbarStore.position`) — die andere Seite meldet `collapsed: true` und ihre Aufrufe sind No-Ops. Hinter `rootSplit`/`leftSplit`/`rightSplit` steht weiterhin kein echter Split-Baum (`children` bleibt leer, Collapse/Expand sind No-Ops). |
| `WorkspaceItem`/`Parent`/`Tabs`/`Root`/`Sidedock`/`MobileDrawer`/`Floating`/`Window`/`Container` | ⚠️ Stub | Alle nur `class WorkspaceLayoutItem {}` — leere `instanceof`-Attrappen ohne Vererbungsbeziehung untereinander. Bewusst nicht weiter ausgebaut: Slatebase konstruiert nirgends eine Instanz davon (`leaf.parent` ist immer `null`, es gibt keinen Split-Baum, kein Popout-Fenster, keine Mobile-Drawer) — eine Vererbungshierarchie wäre für kein Plugin beobachtbar. |
| `HoverPopover` | ⚠️ Stub | `hoverEl` nie im DOM, Konstruktor ignoriert alle Parameter; echtes Hover-Preview läuft separat über `hover-link-bus.ts`. |
| `BaseComponent` / `ValueComponent` / `AbstractTextComponent` | ✅ Vollständig | `onChanged()`-Hook auf `AbstractTextComponent` fehlt — Subklassen-Overrides werden nie erreicht. |
| `TextComponent` / `TextAreaComponent` / `DropdownComponent` | ✅ Vollständig | — |
| `ToggleComponent` | ✅ Vollständig | `onClick()`-Hook fehlt (Klick läuft nur über internen `change`-Listener). |
| `SliderComponent` | ✅ Vollständig | `setLimits()` akzeptiert `number`, `null` und `'any'`; `getValuePretty()` fehlt (Proxy-Fallback). |
| `ColorComponent` | ✅ Vollständig | `getValueRgb()`/`getValueHsl()`/`setValueRgb()`/`setValueHsl()` implementiert, zusätzlich zu Hex. |
| `ProgressBarComponent` | ✅ Vollständig | `setDisabled()` implementiert. |
| `SearchComponent` | ✅ Vollständig | Funktional komplett, aber eigenständig statt `AbstractTextComponent` zu erben (`instanceof` schlägt fehl). |
| `MomentFormatComponent` | ✅ Vollständig | `setPlaceholder()`/`setDisabled()` implementiert. |
| `ExtraButtonComponent` | ✅ Vollständig | `setIcon()` rendert echtes Lucide-/Custom-SVG über eine gemeinsame `renderComponentIcon()`-Hilfsfunktion, geteilt mit `Setting.addExtraButton()`/`SettingGroup.addExtraButton()`. |
| `ButtonComponent` | ✅ Vollständig | Echtes Icon-Rendering; `setDestructive()`/`removeDestructive()` (seit 1.13.0) implementiert. |
| `SecretComponent` | ✅ Vollständig | Minimale Abweichung (`null` statt `''` bei leer), funktional unschädlich. |
| `DisplayValueComponent` | ✅ Vollständig | `setStatus()` (seit 1.13.1) implementiert. |
| `Modal` | ✅ Vollständig | Echtes Overlay/Scope-Push; `setContent()` akzeptiert auch `DocumentFragment`; `setCloseCallback()` implementiert. |
| `SuggestModal` | ✅ Vollständig | `setInstructions()` ist bewusster kosmetischer No-Op. |
| `FuzzySuggestModal` | ✅ Vollständig | Echtes Scoring; `renderSuggestion()` hebt Treffer über die vorhandene `renderMatches()`-Utility hervor. |
| `AbstractInputSuggest` | ✅ Vollständig | Eigener `input`/`keydown`/`blur`-Trigger-Loop (`input-suggest-popover.ts`) — positioniertes Dropdown unter dem `<input>`/`contenteditable`-Feld, ↑↓/Enter/Esc-Navigation, async `getSuggestions()` generation-sicher. `this.suggestEl` ist das echte gerenderte Popover-Element (nicht nur `instanceof`-Attrappe). Tab wird bewusst nicht abgefangen (kein Fokus-Trap in Settings-Feldern). |
| `PopoverSuggest` | ✅ Vollständig | Alle sieben öffentlichen Member aus `obsidian.d.ts` (`app`, `scope`, `constructor`, `open()`, `close()`, `renderSuggestion()`, `selectSuggestion()`) sind vorhanden. `open()`/`close()` sind funktional — sie hängen `suggestEl` in `document.body` ein/aus und togglen `display` — keine leeren No-Ops. `AbstractInputSuggest` überschreibt `suggestEl` mit seinem eigenen Popover-Element, wodurch die geerbten `open()`/`close()` dessen echtes Ein-/Ausblenden übernehmen. `EditorSuggest` rendert sein Popover dagegen komplett separat über `editor-suggest-manager.ts`/`editor-suggest-popover.ts` und ruft die geerbten `open()`/`close()` nie auf — für `EditorSuggest`-Instanzen bleiben sie ungenutzter, aber funktionsfähiger Code. Real Obsidian instanziiert `PopoverSuggest` selbst nie direkt (abstrakte Basisklasse). |
| `Menu` | ✅ Vollständig | Vollständige Item-/Submenu-Logik inkl. `setParentElement()` und `static forEvent()`. |
| `MenuItem` | ✅ Vollständig | Echte Klasse (`frontend/src/plugins/compat/menu.ts`) — `menu.addItem()`-Einträge sind `instanceof MenuItem`. |
| `MenuSeparator` | ✅ Vollständig | Analog zu `MenuItem` — `menu.addSeparator()`-Einträge sind `instanceof MenuSeparator`. |
| `Notice` | ✅ Vollständig | Gesamte offizielle API implementiert: `constructor(message, duration?)`, `setMessage(): this`, `hide()`, `noticeEl`/`messageEl`/`containerEl`. Bridged an das React-Toast-System (`showToast`/`updateToastMessage`/`dismissToast`); `duration: 0` unterdrückt Auto-Dismiss. Bewusster Trade-off: DOM-Elemente sind Platzhalter — direkte DOM-Manipulation daran erscheint nicht im gerenderten Toast (offizielle API sieht nur `setMessage()` für Content-Änderungen vor). |
| `Scope` | ✅ Vollständig | Modifier+Taste-Matching real; `constructor(parent)` kaskadiert nicht zum Parent-Scope. |
| `KeymapEventHandler` / `Keymap` / `SecretStorage` | ✅ Vollständig | — |
| `ConfirmationModal` / `ConfirmationButton` | ✅ Vollständig | — |
| Bases-Wertetypen (`Value`, `BooleanValue`, `DateValue`, …) | ⚠️ Stub | Nur Typ-Stand-ins, `equals()` ist Referenz- statt Wertvergleich, keine typspezifischen Methoden. |
| Bases-Klassen (`BasesView`, `BasesEntry`, …) | ⚠️ Stub | Mehrere Methoden mit **abweichenden Namen** statt bloß zu fehlen (`BasesEntry.getValue()`→`get()`, `BasesEntryGroup.entries`→`items`, …) — zusätzliche Bruchstelle für Plugins, die defensiv auf Methodennamen prüfen. Praktisch folgenlos, da nie eine Bases-Query läuft. |
| `Platform` | ✅ Vollständig | Reale UA/Viewport-Erkennung; `resourcePathPrefix` fehlt **ohne jede Warnung** (kein Proxy, da Plain Object). |

Die Klassifizierung „Proxy-Fallback" vs. „Crash" ist gegen den tatsächlichen Proxy-Einsatz im Code verifiziert (`grep "new Proxy" frontend/src/plugins/compat/`) — nur `App`/`Vault`/`VaultAdapter`/`Workspace`/`MetadataCache`/`FileManager` sind proxy-geschützt, alle UI-Builder- und Modal/Menu-Klassen sind plain JavaScript-Klassen ohne dieses Netz. Alle in der Tabelle oben nicht mehr als **crasht** markierten Methoden sind implementiert.

### Bekannte, bewusst nicht behobene Lücken

Diese Lücken sind echte Feature-Arbeit statt Bugfixes, tragen höheres Risiko in gemeinsam genutztem Code oder sind architektonisch tief verankert:
- **`HoverPopover` — Konstruktor ignoriert Parameter, `hoverEl` nie im DOM**: Echtes Hover-Preview funktioniert bereits, aber über einen separaten Mechanismus (`hover-link-bus.ts`, Event-basiert über `registerHoverLinkSource`), nicht über diese Klasse. Nur relevant für die seltenen Plugins, die `HoverPopover` direkt selbst instanziieren statt nur die Registrierung zu nutzen.
- **Bases-Klassen/Value-Typen**: Weiterhin bewusst nur typisiert (siehe [Was wird unterstützt](#was-wird-unterstützt)) — eine Formel-Engine liegt außerhalb des Scopes dieses Layers.
- **Split-Pane-/Multi-Window-Layout** (`getLayout`/`rootSplit`/Popout-Fenster): Grundsätzliche Architekturentscheidung (flaches Tab-System), keine Einzelmethode zu patchen.
- **Plugin-CSS gegen Slatebase-Kern-DOM**: Das Scoping auf `[data-plugin-id]` lässt Regeln, die Obsidians eigene App-Struktur umgestalten wollen (`.workspace-leaf`, `.markdown-preview-view`, `.tree-item`, `.callout`), bewusst ins Leere laufen — sonst könnte jedes Plugin die gesamte Oberfläche umbauen. Host-Klassen (`theme-dark`, `is-mobile`, …) sind die einzige Ausnahme, weil sie den *Kontext* des Plugin-Elements beschreiben und nicht fremdes DOM. Themes im Obsidian-Sinn (die genau das dürfen) sind kein Bestandteil dieses Layers.
- **`.view-header` ist ein leerer Platzhalter**: `ItemView.containerEl` hat wie in Obsidian zwei Kinder (Header, Content), damit Plugins, die `children[1]` lesen, den Content-Bereich finden. Der Header bleibt aber leer und ungestylt — Slatebase rendert Titel und Aktionen der Tab-Leiste selbst. Plugin-CSS für `.view-header`/`.view-header-title` greift daher auf nichts.
- **Animationsnamen kollidieren**: `@keyframes` wird korrekt *nicht* gescoped (sonst wären die Namen aus den Regeln heraus nicht mehr auflösbar) — dadurch sind sie aber auch nicht namespaced. Zwei Plugins mit gleichnamigen Keyframes überschreiben sich gegenseitig.

---

## Kompatibilitäts-Klassifizierung

Jedes Plugin wird beim Installieren/Hochladen statisch analysiert (`compatibility-analyzer.ts`) und in eines von vier Levels eingeordnet — dieselbe Logik wurde für die Tabelle unten verwendet.

**Leitprinzip:** Grün heißt „verhält sich wie in echtem Obsidian". Gelb heißt „bewusster Stub für nicht vorhandene Funktionalität, warnt garantiert, blockiert die Aktivierung nicht". Rot heißt „Aktivierung wird vermutlich verhindert und/oder es entstehen echte Laufzeitfehler" — die Anwendung als Ganzes stürzt dabei nie ab.

1. **Manifest-Gate**: `isDesktopOnly: true` → sofort **Nicht unterstützt**, keine weitere Analyse.
2. **Node.js-Modul-Erkennung**: `require()`/`import` von Node-Builtins ohne echten Browser-Ersatz (`fs`, `child_process`, `electron`, `stream`, … — nicht `path`/`buffer`, die haben einen echten Shim) wird erkannt und fließt als synthetischer `nodeModule.<name>`-Eintrag mit Klassifizierung „gelb" in dieselbe Liste wie die Obsidian-API-Zugriffe ein (analog zu den `obsidian.Bases`/`obsidian.Cli`-Markern) — schon die bloße Referenz genügt, `isDesktopOnly: true` bleibt aber das stärkere, allein rot-auslösende Signal (viele Bundler ziehen solche Referenzen tot oder optional ein). Das eigentliche Absturz-Sicherheitsnetz sitzt zusätzlich eine Ebene tiefer: der `require()`-Shim im Plugin-Sandbox (`plugin-loader.ts`) liefert für jedes nicht erkannte Modul einen warn-and-no-op-Proxy statt eines rohen `{}` — ein tatsächlicher Methodenaufruf auf einem nicht verfügbaren Node-Modul wirft dadurch keinen ungefangenen `TypeError` mehr, sondern warnt und liefert `undefined`.
3. **API-Zugriffsmuster-Analyse**: Regex-Erkennung von `this.app.<namespace>.<methode>`-Zugriffen, klassifiziert gegen eine Positivliste vollständig emulierter Methoden (`SUPPORTED_METHODS` → grün). Alles andere ist standardmäßig **gelb** (`partial`), nicht rot: jeder erkennbare Namespace (`vault`/`workspace`/`metadataCache`/`plugins`/`fileManager`) ist mit einer Proxy gewrapped, die nicht-emulierte Zugriffe garantiert warnend abfängt statt zu werfen — ein unbekannter Methodenname ist damit per Architektur ein sicherer No-Op, nicht ein Absturzrisiko. Ein separater, aktuell leerer Escape-Hatch (`UNSUPPORTED_METHODS`) erlaubt es, einzelne Methoden explizit als „unsupported" zu markieren, falls ein Shim diese Proxy-Garantie einmal nicht einhalten sollte.
4. **Level-Berechnung**:
   - **Voll kompatibel**: alle erkannten API-Zugriffe sind emuliert (oder keine Zugriffe erkannt).
   - **Teilweise kompatibel**: mindestens ein nicht-emulierter, aber nicht lifecycle-kritischer Zugriff — der sichere Regelfall für „Funktionalität fehlt".
   - **Nicht unterstützt**: ein lifecycle-kritischer Zugriff (`onload`, `onunload`, `Plugin.registerEvent`, `vault.read`, `vault.modify`) ist als `unsupported` klassifiziert (aktuell nur über den `UNSUPPORTED_METHODS`-Escape-Hatch erreichbar, da alle fünf Namen selbst emuliert sind), oder `isDesktopOnly: true`.
   - **Unbekannt**: Bundle leer/stark obfuskiert, Analyse nicht möglich.

Die UI-Badges in der Plugin-Verwaltung heißen entsprechend „Voll kompatibel", „Teilweise kompatibel", „Nicht unterstützt", „Unbekannt".

---

## Top 100 Community-Plugins nach Downloads

**Datenquelle:** `community-plugin-stats.json` + `community-plugins.json` (`obsidianmd/obsidian-releases`, Obsidians eigener aggregierter Download-Feed — derselbe, den Slatebases Plugin-Store selbst nutzt), abgerufen 2026-08-11. „Downloads" ist die kumulative Zähler-Summe über alle Versionen laut diesem Feed.

**Methodik pro Zeile:** `manifest.json` und `main.js` des jeweils *neuesten* GitHub-Release wurden per Skript geladen (`releases/latest/download/…`, 99/100 Repos erfolgreich) und mit einem direkten Port der aktuellen `compatibility-analyzer.ts`-Logik klassifiziert (identische Regex-Muster und Klassifizierungstabellen, inkl. der Node-Modul-als-gelb-Klassifizierung und dem `partial`-Default). Für 12 der Plugins in der Tabelle liegt zusätzlich echte manuelle Testerfahrung vor (Spalte „Manuell getestet"), die bei Abweichungen von der statischen Analyse Vorrang hat (siehe Iconize, Zeile #10).

**Caveats:**
- Statische Analyse ist eine Annäherung, kein Ausführungstest — „Voll kompatibel" heißt „keine bekannte Lücke in den *erkannten* API-Aufrufen", nicht „funktioniert garantiert wie in Obsidian".
- Die Regex-Analyse erkennt nur *literale* `require('modulname')`/`import`-Strings am Aufrufort. Bundler, die Modul-Referenzen in eine generierte Tabelle auslagern (Browserify-Stil, siehe Iconize #10), oder computed/dynamische `require()`-Aufrufe entgehen ihr — das drückt eher zu viele Zeilen fälschlich Richtung „Voll" als umgekehrt.
- „Neuestes Release" auf GitHub ist nicht immer der Haupt-Plugin-Build — bei Repos mit mehreren Release-Kanälen (z. B. Begleitprozesse) kann das zu keinem `main.js` im erwarteten Release führen; in diesem Fall wurde ausschließlich über das Manifest klassifiziert (siehe Media Extended, Zeile #66).
- „Installierbar" bezieht sich auf den Community-Plugin-Store-Browser (Install-Button). Ein manueller ZIP-Upload ist bei Desktop-only-Plugins nicht hart blockiert, aber praktisch nicht lauffähig.
- Eine Node-Modul-*Referenz* zählt bereits als gelb, unabhängig davon, ob der Codepfad zur Laufzeit tatsächlich erreicht wird (die Analyzer-Logik kann das statisch nicht unterscheiden) — ein Teil der als „Teilweise kompatibel" markierten Zeilen könnte daher totes/optionales Bundler-Gepäck betreffen, nicht zwingend echte Laufzeitabhängigkeit.

Die ES5-Downlevel-Basisklassen-Kompatibilität (`_super.call(this, …)`-Aufrufform statt `super()`), der `Buffer`/`path`-Shim und die `ErrorBoundary` um den Plugin-Provider gelten für **alle** Plugins in der Tabelle, nicht nur die mit „Bekannte Einschränkungen"-Einträgen dazu — sie wurden nur durch die manuell getesteten entdeckt.

| # | Plugin | Downloads | Autor | Desktop-only | Installierbar | Kompatibilität | Wartungsstatus | Manuell getestet | Bekannte Einschränkungen |
|---:|---|---:|---|:---:|:---:|---|:---:|:---:|---|
| 1 | [Excalidraw](https://github.com/zsviczian/obsidian-excalidraw-plugin) (`obsidian-excalidraw-plugin`) | 7.133.794 | zsviczian | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | **Excalidraw-spezifischer Stub:** `window.app.internalPlugins.plugins.canvas` ist eigens für Excalidraws `CanvasNodeFactory.initialize()`-Aufrufpfad nachgebaut (`install-globals.ts`) — Slatebase hat zwar eine eigene, native `.canvas`-Ansicht (`components/canvas/CanvasView.tsx`), die ist aber nicht an die Plugin-API angebunden; der Stub verhindert nur den sonst dort auftretenden Absturz, bettet aber nichts wirklich ein (siehe [Embeds](#embeds)). |
| 2 | [Templater](https://github.com/silentvoid13/Templater) (`templater-obsidian`) | 5.212.156 | silentvoid13 | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`child_process`); registriert einen Desktop-CLI-Handler (`obsidian.Cli`, seit 1.12.2 in Slatebase nur typisiert) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 3 | [Dataview](https://github.com/blacksmithgu/obsidian-dataview) (`dataview`) | 4.744.717 | blacksmithgu | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Sonderfälle dokumentiert. |
| 4 | [Tasks](https://github.com/obsidian-tasks-group/obsidian-tasks) (`obsidian-tasks-plugin`) | 3.999.320 | obsidian-tasks-group | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Sonderfälle dokumentiert. |
| 5 | [Advanced Tables](https://github.com/tgrosinger/advanced-tables-obsidian) (`table-editor-obsidian`) | 3.097.039 | Tony Grosinger | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Einschränkungen dokumentiert. |
| 6 | [Calendar](https://github.com/liamcain/obsidian-calendar-plugin) (`calendar`) | 2.988.843 | Liam Cain | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | ✅ Ja (echtes Bundle) | `vault.create()` liefert bei bereits existierendem Pfad die vorhandene Datei zurück statt wie in echtem Obsidian zu werfen (create-or-get statt Reject). |
| 7 | [Git](https://github.com/vinzent03/obsidian-git) (`obsidian-git`) | 2.980.609 | Vinzent | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`child_process`, `fs`, `os`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 8 | [Style Settings](https://github.com/obsidian-community/obsidian-style-settings) (`obsidian-style-settings`) | 2.572.712 | obsidian-community | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 9 | [Kanban](https://github.com/obsidian-community/obsidian-kanban) (`obsidian-kanban`) | 2.517.175 | obsidian-community | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`electron`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 10 | [Iconize](https://github.com/florianwoelki/obsidian-iconize) (`obsidian-icon-folder`) | 2.163.669 | florianwoelki | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Referenziert `fs`/`stream` über eine gebündelte ZIP-Bibliothek fürs Icon-Pack-Herunterladen und ruft davon zur Laufzeit tatsächlich Methoden auf (`readFile`/`readFileSync`) — klassifiziert als `nodeModule.fs`/`nodeModule.stream` (gelb), lädt aber weiterhin und aktiviert sich normal (Sandbox-`require()` liefert einen sicheren No-Op statt zu werfen). **Bekannte Grenze der statischen Analyse:** die Regex-Analyse gegen das echte main.js (v2.14.7) findet hier keinen `require('fs')`/`require('stream')`, weil der Bundler (Browserify-Stil) die Modul-Referenz in eine generierte Tabelle auslagert (`e("stream")` statt eines Literal-Strings am Aufrufort) — der Live-Test in der laufenden App bleibt hier die verlässlichere Quelle. |
| 11 | [Remotely Save](https://github.com/remotely-save/remotely-save) (`remotely-save`) | 2.131.499 | remotely-save | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 12 | [QuickAdd](https://github.com/chhoumann/quickadd) (`quickadd`) | 1.976.743 | chhoumann | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 13 | [Minimal Theme Settings](https://github.com/kepano/obsidian-minimal-settings) (`obsidian-minimal-settings`) | 1.732.008 | kepano | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 14 | [Editing Toolbar](https://github.com/pkm-er/obsidian-editing-toolbar) (`editing-toolbar`) | 1.726.740 | pkm-er | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`http`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 15 | [Omnisearch](https://github.com/scambier/obsidian-omnisearch) (`omnisearch`) | 1.724.246 | Simon Cambier | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`http`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 16 | [Claudian](https://github.com/yishentu/claudian) (`realclaudian`) | 1.722.530 | Yishen Tu | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 17 | [Copilot](https://github.com/logancyang/obsidian-copilot) (`copilot`) | 1.657.537 | Logan Yang | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`child_process`, `crypto`, `electron`, `fs`, `https`, `os`, `readline`, `stream`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 18 | [Importer](https://github.com/obsidianmd/obsidian-importer) (`obsidian-importer`) | 1.513.333 | Obsidian | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`child_process`, `crypto`, `fs`, `original-fs`, `os`, `stream`, `zlib`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. Der Format-Auswahl-Dialog (Command „Notizen importieren") öffnet sich seit der Ergänzung von `Setting.setNavigable()`/`setAction()`/`setIcon()` (Obsidian 1.13, undokumentiert) samt Format-Liste, Suche und Quell-Auswahl. Der eigentliche Import läuft damit noch nicht durch: das Plugin nutzt dafür `vault.getAvailablePath()`, `vault.getAbstractFileByPathInsensitive()`, `vault.getConfig()` und `app.metadataTypeManager` — allesamt undokumentierte Runtime-APIs, die im Vault-Shim nur als No-op existieren. |
| 19 | [Outliner](https://github.com/vslinko/obsidian-outliner) (`obsidian-outliner`) | 1.340.550 | vslinko | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Prüft `window.CodeMirrorAdapter.Vim` (Obsidians `@replit/codemirror-vim`-Global) für sein optionales „Vim `o`/`O`-Verhalten überschreiben"-Feature, das als No-Op-Stub emuliert wird — harmlos (Plugin lädt und der Rest funktioniert normal), aber ohne echte Vim-Normalmodus-Befehle. |
| 20 | [Homepage](https://github.com/mirnovov/obsidian-homepage) (`homepage`) | 1.266.646 | mirnovov | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin registriert einen Desktop-CLI-Handler (`obsidian.Cli`, seit 1.12.2 in Slatebase nur typisiert) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 21 | [Recent Files](https://github.com/tgrosinger/recent-files-obsidian) (`recent-files-obsidian`) | 1.149.567 | Tony Grosinger | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Einschränkungen dokumentiert. |
| 22 | [Smart Connections](https://github.com/brianpetro/obsidian-smart-connections) (`smart-connections`) | 1.141.681 | 🌴 Brian | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `plugins.installPlugin` ist nicht emuliert (bewusst, siehe [Was wird nicht unterstützt](#was-wird-nicht-unterstützt)). |
| 23 | [Tag Wrangler](https://github.com/pjeby/tag-wrangler) (`tag-wrangler`) | 1.025.748 | pjeby | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 24 | [Linter](https://github.com/platers/obsidian-linter) (`obsidian-linter`) | 1.013.436 | platers | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 25 | [Admonition](https://github.com/ebullient/obsidian-admonition) (`obsidian-admonition`) | 945.792 | ebullient | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `app.plugins.getPluginFolder()` (Dateisystempfad-Auskunft) ist nicht emuliert — Slatebase hat kein lokales Dateisystem für Plugins. |
| 26 | [BRAT](https://github.com/tfthacker/obsidian42-brat) (`obsidian42-brat`) | 943.538 | tfthacker | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `plugins.installPlugin` (Beta-Installation direkt von einem GitHub-Repo, BRATs Kernfunktion) ist bewusst nicht emuliert, siehe [Was wird nicht unterstützt](#was-wird-nicht-unterstützt). |
| 27 | [TaskNotes](https://github.com/callumalpass/tasknotes) (`tasknotes`) | 939.206 | callumalpass | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`http`, `stream`); registriert einen Desktop-CLI-Handler (`obsidian.Cli`, seit 1.12.2 in Slatebase nur typisiert); referenziert Obsidian Bases (`obsidian.Bases`, keine Formel-Engine/`.base`-Rendering) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 28 | [Mind Map](https://github.com/lynchjames/obsidian-mind-map) (`obsidian-mind-map`) | 869.529 | lynchjames | Nein | Ja | Voll kompatibel | ⚠️ Kaputt (bestätigt) | ✅ Ja (echtes Bundle) | Der Preview-Befehl öffnet die Mind-Map-Ansicht fehlerfrei; keine plugin-spezifischen Einschränkungen der Compat-Schicht selbst dokumentiert (zum bestätigt kaputten Upstream-Zustand siehe Wartungsstatus-Spalte). |
| 29 | [Day Planner](https://github.com/ivan-lednev/obsidian-day-planner) (`obsidian-day-planner`) | 855.083 | ivan-lednev | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Sonderfälle dokumentiert. |
| 30 | [Self-hosted LiveSync](https://github.com/vrtmrz/obsidian-livesync) (`obsidian-livesync`) | 851.999 | vrtmrz | Nein | Ja | Teilweise kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`crypto`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. `adapter.mkdir()`, das LiveSync als Vorbereitungsschritt vor dem Schreiben aufruft, ist ein No-Op (das Backend legt Verzeichnisse automatisch beim Schreiben einer Datei an). |
| 31 | [make.md](https://github.com/make-md/makemd) (`make-md`) | 851.093 | make-md | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 32 | [Notebook Navigator](https://github.com/johansan/notebook-navigator) (`notebook-navigator`) | 838.390 | Johan Sanneblad | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`electron`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 33 | [Advanced Slides](https://github.com/mszturc/obsidian-advanced-slides) (`obsidian-advanced-slides`) | 833.152 | mszturc | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 34 | [Periodic Notes](https://github.com/liamcain/obsidian-periodic-notes) (`periodic-notes`) | 735.568 | Liam Cain | Nein | Ja | Voll kompatibel | ⚠️ Kaputt (bestätigt) | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 35 | [Advanced Canvas](https://github.com/developer-mike/obsidian-advanced-canvas) (`advanced-canvas`) | 724.860 | mika-dev | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | **Vermutlich falsch positiv:** Statische Analyse findet keine nicht-emulierten Zugriffe, aber die Regex-Erkennung sucht nur nach `this.app.<namespace>.<methode>`-Mustern — tief verschachtelte Pfade wie `internalPlugins.plugins.canvas.*` (auf die dieses Plugin praktisch vollständig angewiesen ist) erkennt sie gar nicht als API-Zugriff. Diese Internal-API ist in Slatebase aktuell reiner Absturz-Schutz, nichts wird wirklich eingebettet oder manipuliert (siehe „Geplant, aber noch nicht implementiert" oben) — das Plugin dürfte in der Praxis weitgehend wirkungslos bleiben, ungeachtet des Badges. |
| 36 | [Highlightr](https://github.com/chetachiezikeuzor/Highlightr-Plugin) (`highlightr-plugin`) | 696.665 | chetachiezikeuzor | Nein | Ja | Voll kompatibel | ⚠️ Kaputt (bestätigt) | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 37 | [PDF++](https://github.com/ryotaushio/obsidian-pdf-plus) (`pdf-plus`) | 679.335 | ryotaushio | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `workspace.getGroupLeaves` ist nicht emuliert. `metadataCache.onCleanCache` ist nicht emuliert. |
| 38 | [Local REST API with MCP](https://github.com/coddingtonbear/obsidian-local-rest-api) (`obsidian-local-rest-api`) | 653.801 | Adam Coddington | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 39 | [Advanced URI](https://github.com/vinzent03/obsidian-advanced-uri) (`obsidian-advanced-uri`) | 629.492 | Vinzent | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Update-Check über `app.plugins` ist nicht emuliert (Slatebase hat einen eigenen Update-Mechanismus im Plugin-Store). Update-Status über `app.plugins.updates` ist nicht emuliert. |
| 40 | [Commander](https://github.com/jsmorabito/obsidian-commander) (`cmdr`) | 615.844 | jsmorabito | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 41 | [Better Word Count](https://github.com/lukeleppan/better-word-count) (`better-word-count`) | 602.051 | lukeleppan | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2025 | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Einschränkungen dokumentiert. |
| 42 | [Annotator](https://github.com/elias-sundqvist/obsidian-annotator) (`obsidian-annotator`) | 589.345 | elias-sundqvist | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 43 | [Markmind](https://github.com/markmindckm/obsidian-markmind) (`obsidian-markmind`) | 584.471 | markmindckm | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `app.plugins.getPluginFolder()` (Dateisystempfad-Auskunft) ist nicht emuliert — Slatebase hat kein lokales Dateisystem für Plugins. |
| 44 | [Spaced Repetition](https://github.com/st3v3nmw/obsidian-spaced-repetition) (`obsidian-spaced-repetition`) | 576.380 | st3v3nmw | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 45 | [Text Generator](https://github.com/nhaouari/obsidian-textgenerator-plugin) (`obsidian-textgenerator-plugin`) | 566.508 | nhaouari | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Popout-Fenster existieren in Slatebase nicht (Browser-Tab-System statt Multi-Window). |
| 46 | [Various Complements](https://github.com/tadashi-aikawa/obsidian-various-complements-plugin) (`various-complements`) | 554.458 | tadashi-aikawa | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 47 | [Hover Editor](https://github.com/nothingislost/obsidian-hover-editor) (`obsidian-hover-editor`) | 545.798 | nothingislost | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Drag&Drop-Reordering von Leaves ist nicht emuliert (kein Split-/Fenstersystem). |
| 48 | [Pandoc Plugin](https://github.com/oliverbalfour/obsidian-pandoc) (`obsidian-pandoc`) | 542.232 | oliverbalfour | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 49 | [Zotero Integration](https://github.com/obsidian-community/obsidian-zotero-integration) (`obsidian-zotero-desktop-connector`) | 532.313 | obsidian-community | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 50 | [Latex Suite](https://github.com/artisticat1/obsidian-latex-suite) (`obsidian-latex-suite`) | 526.719 | artisticat | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`fs`, `os`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 51 | [Natural Language Dates](https://github.com/obsidian-community/nldates) (`nldates-obsidian`) | 502.978 | obsidian-community | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 52 | [Image Converter](https://github.com/xryul/obsidian-image-converter) (`image-converter`) | 494.356 | xryul | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`child_process`, `fs`, `os`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 53 | [Emoji Toolbar](https://github.com/oliveryh/obsidian-emoji-toolbar) (`obsidian-emoji-toolbar`) | 486.112 | oliveryh | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 54 | [Paste URL into selection](https://github.com/denolehov/obsidian-url-into-selection) (`url-into-selection`) | 483.459 | denolehov | Nein | Ja | Voll kompatibel | Aktiv | ✅ Ja (echtes Bundle) | Keine plugin-spezifischen Einschränkungen dokumentiert. |
| 55 | [Meta Bind](https://github.com/mprojectscode/obsidian-meta-bind-plugin) (`obsidian-meta-bind-plugin`) | 465.616 | Moritz Jung | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`crypto`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 56 | [Checklist](https://github.com/delashum/obsidian-checklist-plugin) (`obsidian-checklist-plugin`) | 454.727 | delashum | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2025 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 57 | [Full Calendar](https://github.com/obsidian-community/obsidian-full-calendar) (`obsidian-full-calendar`) | 450.525 | obsidian-community | Nein | Ja | Voll kompatibel | ⚠️ Archiviert | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 58 | [Hider](https://github.com/kepano/obsidian-hider) (`obsidian-hider`) | 439.736 | kepano | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 59 | [Quick Switcher++](https://github.com/darlal/obsidian-switcher-plus) (`darlal-switcher-plus`) | 430.148 | darlal | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`electron`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 60 | [Enhancing Export](https://github.com/mokeyish/obsidian-enhancing-export) (`obsidian-enhancing-export`) | 430.092 | mokeyish | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 61 | [Buttons](https://github.com/shabegom/buttons) (`buttons`) | 429.790 | shabegom | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 62 | [Thino](https://github.com/quorafind/Obsidian-Thino) (`obsidian-memos`) | 400.549 | Boninall | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin registriert einen Desktop-CLI-Handler (`obsidian.Cli`, seit 1.12.2 in Slatebase nur typisiert) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 63 | [Folder notes](https://github.com/lostpaul/obsidian-folder-notes) (`folder-notes`) | 399.920 | lostpaul | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 64 | [Terminal](https://github.com/polyipseity/obsidian-terminal) (`terminal`) | 385.706 | polyipseity | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`crypto`, `fs`, `os`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 65 | [Auto Link Title](https://github.com/zolrath/obsidian-auto-link-title) (`obsidian-auto-link-title`) | 373.758 | zolrath | Nein | Ja | Teilweise kompatibel | ⚠️ Kaputt (bestätigt) | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`electron`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 66 | [Media Extended](https://github.com/aidenlx/media-extended) (`media-extended`) | 359.796 | Aiden Liu | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). **Hinweis zum automatisierten Re-Audit (2026-08-16):** `releases/latest` zeigt bei diesem Repo auf einen separaten Companion-Release-Kanal (`main-daemon`, Tag `main-daemon-1.0.3`) ohne `manifest.json`/`main.js`-Assets — automatischer Abruf schlug fehl (404). Gegenprobe direkt gegen `manifest.json` im `master`-Branch bestätigt weiterhin `isDesktopOnly: true`, das Verdikt bleibt also unverändert. |
| 67 | [Tracker](https://github.com/pyrochlore/obsidian-tracker) (`obsidian-tracker`) | 356.856 | pyrochlore | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 68 | [Image in Editor](https://github.com/ozntel/oz-image-in-editor-obsidian) (`oz-image-plugin`) | 352.286 | ozntel | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 69 | [File Tree Alternative](https://github.com/ozntel/file-tree-alternative) (`file-tree-alternative`) | 344.950 | ozntel | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 70 | [Banners](https://github.com/noatpad/obsidian-banners) (`obsidian-banners`) | 342.089 | noatpad | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 71 | [Note Refactor](https://github.com/lynchjames/note-refactor-obsidian) (`note-refactor-obsidian`) | 338.702 | lynchjames | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 72 | [Easy Typing](https://github.com/yaozhuwa/easy-typing-obsidian) (`easy-typing-obsidian`) | 332.716 | yaozhuwa | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 73 | [ExcaliBrain](https://github.com/zsviczian/excalibrain) (`excalibrain`) | 329.887 | zsviczian | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `metadataCache.inProgressTaskCount` (Indexierungs-Fortschritt) ist nicht emuliert — Slatebase indiziert synchron on-demand. |
| 74 | [Mermaid Tools](https://github.com/dartungar/obsidian-mermaid) (`mermaid-tools`) | 323.493 | Daniel Nikolaev | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 75 | [Fantasy Statblocks](https://github.com/obsidian-ttrpg-community/fantasy-statblocks) (`obsidian-5e-statblocks`) | 319.139 | obsidian-ttrpg-community | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 76 | [Quiet Outline](https://github.com/guopenghui/obsidian-quiet-outline) (`obsidian-quiet-outline`) | 316.867 | guopenghui | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `fileManager.iterateAllRefs()` (Referenz-Iteration über den ganzen Vault) ist nicht emuliert. |
| 77 | [Text Extractor](https://github.com/scambier/obsidian-text-extractor) (`text-extractor`) | 316.782 | Simon Cambier | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`child_process`, `electron`, `os`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 78 | [Charts](https://github.com/phibr0/obsidian-charts) (`obsidian-charts`) | 316.216 | phibr0 | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 79 | [Reminder](https://github.com/uphy/obsidian-reminder) (`obsidian-reminder-plugin`) | 313.898 | uphy | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Plugin referenziert Node-Built-ins ohne Browser-Ersatz (`electron`) — sicher gestubbt (Warnung, kein Absturz), Badge daher gelb statt grün. |
| 80 | [Datacore](https://github.com/blacksmithgu/datacore) (`datacore`) | 311.856 | blacksmithgu | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 81 | [Better Export PDF](https://github.com/l1xnan/obsidian-better-export-pdf) (`better-export-pdf`) | 308.989 | l1xnan | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 82 | [Metadata Menu](https://github.com/mdelobelle/metadatamenu) (`metadata-menu`) | 306.291 | mdelobelle | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `metadataCache.inProgressTaskCount` (Indexierungs-Fortschritt) ist nicht emuliert — Slatebase indiziert synchron on-demand. |
| 83 | [Leaflet](https://github.com/javalent/obsidian-leaflet) (`obsidian-leaflet-plugin`) | 301.953 | javalent | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2025 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 84 | [Note Toolbar](https://github.com/chrisgurney/obsidian-note-toolbar) (`note-toolbar`) | 300.668 | chrisgurney | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `vault.setConfig()` (Schreibzugriff auf Obsidians App-Settings, z.B. `readableLineLength`) ist nicht emuliert — nur lesend über `getConfig()`. |
| 85 | [LanguageTool Integration](https://github.com/clemens-e/obsidian-languagetool-plugin) (`obsidian-languagetool-plugin`) | 298.983 | clemens-e | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | `vault.setConfig()` (Schreibzugriff auf Obsidians App-Settings, z.B. `readableLineLength`) ist nicht emuliert — nur lesend über `getConfig()`. |
| 86 | [File Explorer Note Count](https://github.com/ozntel/file-explorer-note-count) (`file-explorer-note-count`) | 285.506 | ozntel | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 87 | [Dice Roller](https://github.com/obsidian-ttrpg-community/dice-roller) (`obsidian-dice-roller`) | 277.103 | obsidian-ttrpg-community | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2025 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 88 | [Breadcrumbs](https://github.com/michaelpporter/breadcrumbs) (`breadcrumbs`) | 275.522 | Michael Porter | Nein | Ja | Teilweise kompatibel | Aktiv | – Nur statische Analyse | Obsidians natives Link-Kontextmenü-Hook ist nicht emuliert. `metadataCache.initialized`-Flag ist nicht emuliert. |
| 89 | [Colored Text](https://github.com/erincayaz/obsidian-colored-text) (`colored-text`) | 269.404 | erincayaz | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 90 | [Custom Frames](https://github.com/ellpeck/ObsidianCustomFrames) (`obsidian-custom-frames`) | 253.639 | Ell | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 91 | [Pretty Properties](https://github.com/anareaty/pretty-properties) (`pretty-properties`) | 252.458 | Reaty | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 92 | [TagFolder](https://github.com/vrtmrz/obsidian-tagfolder) (`obsidian-tagfolder`) | 244.245 | vrtmrz | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 93 | [Readwise Official](https://github.com/readwiseio/obsidian-readwise) (`readwise-official`) | 239.728 | readwiseio | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 94 | [cMenu](https://github.com/chetachiezikeuzor/cMenu-Plugin) (`cmenu-plugin`) | 235.117 | chetachiezikeuzor | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | ⚠️ Inaktiv seit 2022 | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 95 | [Enhancing Mindmap](https://github.com/markmindckm/obsidian-enhancing-mindmap) (`obsidian-enhancing-mindmap`) | 233.704 | markmindckm | Nein | Ja | Voll kompatibel | Aktiv | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 96 | [Citations](https://github.com/hans/obsidian-citation-plugin) (`obsidian-citation-plugin`) | 231.512 | hans | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 97 | [Code Styler](https://github.com/mayurankv/Obsidian-Code-Styler) (`code-styler`) | 226.388 | mayurankv | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2025 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 98 | [Book Search](https://github.com/anpigon/obsidian-book-search-plugin) (`obsidian-book-search-plugin`) | 225.723 | anpigon | Nein | Ja | Voll kompatibel | ⚠️ Inaktiv seit 2024 | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |
| 99 | [Agent Client](https://github.com/rait-09/obsidian-agent-client) (`agent-client`) | 224.225 | rait-09 | Ja | Nein (Store blockiert Install-Button) | Nicht unterstützt | Aktiv | – Nur statische Analyse | Desktop-only (`manifest.json`: `isDesktopOnly: true`) — Node.js/Electron-APIs nicht im Browser verfügbar. Installation über den Plugin Store ist blockiert (kein Install-Button). |
| 100 | [Dictionary](https://github.com/phibr0/obsidian-dictionary) (`obsidian-dictionary-plugin`) | 223.158 | phibr0 | Nein | Ja | Voll kompatibel | ⚠️ Verwaist | – Nur statische Analyse | Statische Analyse: alle erkannten API-Zugriffe vollständig emuliert. |

---

## Wartungsstatus der Top-100-Plugins (Upstream-Pflege)

**Datum:** 2026-08-15
**Warum das eine eigene Achse ist:** Die Kompatibilitäts-Klassifizierung oben (voll/teilweise/nicht unterstützt) prüft nur, ob Slatebase die vom Plugin genutzten API-Aufrufe emuliert. Sie sagt nichts darüber aus, ob das Plugin *upstream*, also unter echtem, aktuellem Obsidian, überhaupt noch fehlerfrei läuft. Ein als „Voll kompatibel" eingestuftes Plugin ist wertlos, wenn es bereits im echten Obsidian kaputt ist — Slatebase kann ein upstream-defektes Plugin nicht heil emulieren.

**Methodik:** Für alle 100 Repos wurde per GitHub-API (`archived`, `pushed_at`) der letzte Commit-Zeitpunkt geprüft (Stichtag 2026-08-15). Als **auffällig** gilt: `archived: true`, ein umbenanntes/verwaistes Repo, oder kein Push seit mehr als ~12 Monaten (vor 2025-08). Für jeden auffälligen Treffer wurde zusätzlich in den Issues nach expliziten „funktioniert nicht mehr"/Versions-Beschwerden gesucht. Plugins mit Aktivität in den letzten Monaten wurden nicht weiter untersucht.

**Wichtiger Vorbehalt:** Inaktivität ist ein Risikosignal, kein Beweis für Bruch. Von den 24 auffälligen Repos liegt nur bei 4 ein konkretes GitHub-Issue vor, das eine tatsächliche Fehlfunktion unter aktuellem Obsidian bestätigt. Bei den übrigen 20 wurde keine Bruchmeldung gefunden — sie könnten weiterhin fehlerfrei laufen, tragen aber ohne aktiven Maintainer ein erhöhtes Risiko, bei der nächsten Obsidian-Version stillschweigend zu brechen.

### Bestätigt kaputt unter aktuellem Obsidian (4)

| # | Plugin | Repo | Befund |
|---:|---|---|---|
| 36 | Highlightr | [chetachiezikeuzor/Highlightr-Plugin](https://github.com/chetachiezikeuzor/Highlightr-Plugin) | Letzter Push 2023-11-15 (~2,75 Jahre). Issue #112 (08/2026): Settings-Tab (Farbwähler/Save-Button) rendert nicht unter **Obsidian 1.13.x**. Seit #91 (2024) offene Maintainer-Suche. |
| 34 | Periodic Notes | [liamcain/obsidian-periodic-notes](https://github.com/liamcain/obsidian-periodic-notes) | Letzter Push 2024-08-23 (~2 Jahre). Issue #249 (02/2025): „Obsidian Community Plugin Version Too Old". |
| 65 | Auto Link Title | [zolrath/obsidian-auto-link-title](https://github.com/zolrath/obsidian-auto-link-title) | Letzter Push 2024-12-15 (~20 Monate). Issue #163 (09/2025): „Not working with the latest Obsidian version". Zusätzlich #177 (08/2026): Sicherheitsproblem (Malvertising im versteckten Browser-Fenster) — unabhängig vom Kompatibilitätsthema, aber ein weiterer Grund zur Vorsicht. |
| 28 | Mind Map | [lynchjames/obsidian-mind-map](https://github.com/lynchjames/obsidian-mind-map) | Letzter Push 2024-02-25 (~2,5 Jahre). #117 (04/2025): „Doesn't work in latest version"; #119 (09/2025): „Obsidian update." |

### Formal aufgegeben (2)

| # | Plugin | Repo | Status |
|---:|---|---|---|
| 57 | Full Calendar | [obsidian-community/obsidian-full-calendar](https://github.com/obsidian-community/obsidian-full-calendar) | **Repo archiviert** (GitHub `archived: true`), letzter Push 2024-11-08. Keine Fixes mehr möglich, unabhängig vom aktuellen Fehlerstatus. |
| 100 | Dictionary | [phibr0/obsidian-dictionary](https://github.com/phibr0/obsidian-dictionary) | Repo umbenannt/verwaist, letzter Push ~2024-02 (~2,5 Jahre). Keine versionsspezifische Bruchmeldung gefunden. |

### Über 12 Monate inaktiv, keine bestätigte Bruchmeldung (18)

| # | Plugin | Repo | Letzter Push |
|---:|---|---|---|
| 6 | Calendar | liamcain/obsidian-calendar-plugin | 2024-06-22 (~2 Jahre) |
| 11 | Remotely Save | remotely-save/remotely-save | 2024-11-10 (~21 Monate) |
| 33 | Advanced Slides | mszturc/obsidian-advanced-slides | 2024-06-29 (~2 Jahre) |
| 41 | Better Word Count | lukeleppan/better-word-count | 2025-06-20 (~14 Monate) |
| 42 | Annotator | elias-sundqvist/obsidian-annotator | 2024-01-08 (~2,5 Jahre) |
| 48 | Pandoc Plugin | oliverbalfour/obsidian-pandoc | 2024-05-15 (~2,25 Jahre) |
| 56 | Checklist | delashum/obsidian-checklist-plugin | 2025-01-11 (~19 Monate) |
| 68 | Image in Editor | ozntel/oz-image-in-editor-obsidian | 2024-02-10 (~2,5 Jahre) |
| 69 | File Tree Alternative | ozntel/file-tree-alternative | 2024-06-22 (~2 Jahre) |
| 70 | Banners | noatpad/obsidian-banners | 2024-01-18 (~2,5 Jahre) |
| 71 | Note Refactor | lynchjames/note-refactor-obsidian | 2024-01-22 (~2,5 Jahre) |
| 78 | Charts | phibr0/obsidian-charts | 2024-06-19 (~2 Jahre) |
| 83 | Leaflet | javalent/obsidian-leaflet | 2025-07-09 (~13 Monate) |
| 87 | Dice Roller | obsidian-ttrpg-community/dice-roller | 2025-03-24 (~17 Monate) |
| 94 | cMenu | chetachiezikeuzor/cMenu-Plugin | 2022-09-05 (~4 Jahre, älteste im gesamten Set) |
| 96 | Citations | hans/obsidian-citation-plugin | 2024-06-13 (~2 Jahre) |
| 97 | Code Styler | mayurankv/Obsidian-Code-Styler | 2025-02-16 (~18 Monate) |
| 98 | Book Search | anpigon/obsidian-book-search-plugin | 2024-10-16 (~22 Monate); Issues nur zu API-Rate-Limits, nicht versionsbezogen |

**Zusammenfassung:** 24 von 100 auffällig (1 archiviert, 1 verwaist/umbenannt, 22 mit ≥12 Monaten ohne Commit), davon 4 mit bestätigter Fehlfunktion unter aktuellem Obsidian. Die übrigen 76 zeigen Aktivität innerhalb der letzten Monate. Dieser Befund ist unabhängig von Slatebases eigener API-Aktualität — beide sollten in der Plugin-Store-UI als Warnhinweis sichtbar gemacht werden, bevor Nutzer eines der 24 Plugins installieren.

---

*Dieses Dokument wurde aus dem aktuellen Stand der Compat-Schicht (`frontend/src/plugins/compat/compatibility-analyzer.ts`), den Projekt-Steering-Docs (`.kiro/steering/product.md`, `.kiro/steering/lessons-learned.md`) und einer automatisierten Analyse der 100 meistheruntergeladenen Community-Plugins erzeugt. Bei Abweichungen zwischen diesem Dokument und dem tatsächlichen Verhalten der App gilt der Code als Quelle der Wahrheit.*
